# Solenoid 1.4 — release plan (PROPOSAL, 2026-09-01)

> **STATUS: PROPOSAL — awaiting the author's cut.** 1.3 shipped (v1.3.0 on `main`). This is
> the planning pass the backlog's "Deferral review" item asked for: every parked, deferred and
> author-gated idea walked, scored, and either planned here, planned in [`2.0-plan.md`](2.0-plan.md)
> (the structural arcs), or left in [`deferrals.md`](deferrals.md) (parked, no plan). Nothing here
> is scheduled until the author promotes it: a promoted item becomes a line in
> [`backlog.md`](backlog.md) (the task truth) and its plan section here is the spec. When an
> item lands, delete its section; when the release ships, this file archives.

**How to read a plan.** Each item carries a score (rubric below), the grounding (where it
lives in code, what already exists), the build steps, the tests that pin it, the gate (an
author call, a license, a design pass) and a done-definition. Line numbers are deliberately
absent — grep the symbol. **No time estimates anywhere, by order.**

## Theme

1.3 finished the engine body (parity, the analytics shelf, Script, computed columns, the
React Flow port). **1.4 is the workbench release: the canvas becomes a place you *operate* a
model, not only build one.** Four ideas carry it:

1. **What-if surgery** — pin, mute, peek, and a hover brush that shows a node's cone, plus an
   Optimize run mode so "min/max this subject to that" stops being the one Solver-shaped hole.
2. **Records and tables, finished** — the Record node's view family and the table popup get
   the display lifts the competitor sweep found, and categorical columns get their 1.4 look.
3. **Everyday sources** — the widget nodes (Weather / Geocode / Holidays / FX / TZ / QR) that
   make a document worth leaving open, behind a per-document network permission.
4. **Surface hardening** — the formula surface becomes declared-only (allowlist), the rules
   get their ARR pass, node-combining round 2 lands, and the AI palette comes back on.

Not 1.4, by design: anything that changes what a *document* is (pages, cloud, multiplayer),
the Excel transpiler, conditional formatting, the canvas-at-scale rework — `2.0-plan.md`.

## The rubric (used here and in `2.0-plan.md`)

| Axis | Values | What it measures |
|---|---|---|
| **Strength** | Strong / Solid / Weak | How much the idea improves the product on its own terms — would a user go find it and play with it (the release-notes bar) or does it close a real hole |
| **Relevance** | Core / Adjacent / Peripheral | Fit to the identity sentence — *inspectable, typed computation over data, in a file the user owns* — and to this release's theme |
| **Complexity** | Low / Medium / High / Very high | Design plus build difficulty. NOT time |
| **Blast radius** | Contained / Moderate / Wide / Structural | Contained = one module and its test. Moderate = one subsystem, a few docs. Wide = several subsystems, or the save format, socket lattice, or value model. Structural = touches identity, `decisions.md`, or an architecture seam |
| **Call** | IN / PULLABLE / AUTHOR / HOLD / DROP / 2.0 | IN = recommended for 1.4. PULLABLE = fits 1.4 if capacity allows. AUTHOR = cannot be placed without a decision. HOLD = stays parked with its trigger. DROP = recommend ruling it out. 2.0 = planned in `2.0-plan.md` |

## The cut at a glance

| # | Item | Strength | Relevance | Complexity | Blast radius | Call |
|---|---|---|---|---|---|---|
| A0 | Node-state badge design pass (new) | Solid | Core | Low | Moderate (DESIGN.md, `NodeCard`) | DROPPED with A1/A2 (2026-09-04c) |
| A1 | Pin a node's output | Strong | Core | Medium | Moderate (compute pass, persistence, card) | DEFERRED (author 2026-09-04c; `deferrals.md`) |
| A2 | Mute / bypass a node | Solid | Core | Medium | Moderate (lattice pass-through rules) | DEFERRED (author 2026-09-04c; `deferrals.md`) |
| A3 | Peek any socket | Strong | Core | Low | Contained (socket hover + the frame hint layer) | PROMOTED, reshaped (author 2026-09-04c: a hover peek, not a menu verb; spec in `backlog.md`) |
| A4 | Dependency-cone hover brush | Solid | Core | Low | Contained (`isolate.ts` cone walk, `cableState`) | DEFERRED (author 2026-09-04c; `deferrals.md`) |
| A5 | Fusion indicator | Weak | Adjacent | Low | Contained (`CableInspector`) | PULLABLE |
| A6 | Optimize run mode (new — replaces "constrained optimizer node") | Strong | Core | High | Moderate (composite run modes) | IN, LATER (author 2026-09-04c: in, not now; starts on the author's word) |
| B1 | Record views polish tranche | Solid | Adjacent | Medium | Moderate (`nodes/visual.ts` Record + `RecordNode.tsx`) | DONE 2026-09-04c as trimmed by the author (size presets gallery-only, indented List view, `#field` title row, clamp); OUT = cover image, hide-empty, grouped gallery + lane summaries |
| B2 | Select / categorical columns (display chips + constrained entry) | Solid | Core | Medium | Moderate (`TablePopup`, `frameFormatStore`, the FC text styles) | PROMOTED 2026-09-04c: both halves IN, chips ALSO a style on the standard FC (author); the enum column TYPE stays 2.0 (author) |
| B3 | Table popup lifts | Solid | Adjacent | Low–Medium | Contained | DONE 2026-09-04c as trimmed by the author (17f1195a frozen header via the popup menu, summary-footer row out of Settings; 68cc31e4 record arrow-key nav). OUT = image lightbox, chip hover preview |
| B4 | Computed Column UX tail | Solid | Core | Medium | Moderate | DONE 2026-09-04c (75c7ea2b: the shared column picker on Sort / Get Column / Join); OUT = card-side format/unit; DEFERRED = λ view-as (`deferrals.md`) |
| B5 | XMATCH / XLOOKUP remainder (1×N matrix lookup value; per-arg spill) | Weak | Core | Medium | Contained (`xmatchIndex`, matrix gate) | PULLABLE |
| B6 | Peek dock (side-docked popups) | Solid | Adjacent | Medium | Moderate (popup hosts, `layout-chrome.md`) | AUTHOR |
| B7 | Gallery tile click → picks the row | Solid | Adjacent | Medium | Moderate (a figure gaining an input gesture) | AUTHOR |
| B8 | Two-axis board (swimlanes); color-by field; calendar figure; Gantt | Solid | Adjacent | High | Moderate | 2.0 (Gantt/calendar) · HOLD (swimlanes) · with CF (color-by) |
| B9 | Table/cube popup virtualization for wide EDITABLE frames | Weak | Adjacent | Medium | Moderate | HOLD (author: "don't really care") |
| C1 | Widget nodes Tier 1 (`v2.0/16`) | Strong | Core | Medium | Moderate (connection pattern, packs) | PROMOTED 2026-09-04c (author: in); the four calls default to the plan's recommendations (FX yes; the three keyless providers as built-ins; fetching four beside Connections, pure two in Timesavers; Garden Dashboard seed) unless the author overrules |
| C2 | Per-document network permission (new) | Solid | Core | Low | Moderate (`connectionStore`, load path) | DONE 2026-09-04c (b5725d52: foreign docs gated, one Allow notice, per-doc grant in the sidecar, Settings ▸ Data escape hatch) |
| C3 | Data Feed widening (symbol search, providers) | Weak | Adjacent | Medium | Contained (`dataFeed.ts`) | HOLD |
| C4 | iFrame / embed node | Weak | Peripheral | Medium | Moderate (CSP) | DEFERRED (author 2026-09-04c; `deferrals.md`) |
| C5 | Timesaver date idioms needing config (Fiscal Quarter, Age, Nth Weekday) | Solid | Core | Low | Contained (`packs/timesavers.ts`) | DONE 2026-09-04c (cd1de172: Quarter `start`, Age, Nth Weekday) |
| D1 | Formula surface allowlist — Option A DROPPED on step-0 findings (2026-09-04c); it regresses 127 correct broadcasts. Option B (D1b) is the path, author-present | — | Core | — | — | DROPPED |
| D1b | Formula surface allowlist — Option B audit | Solid | Core | Medium | Moderate (author-in-the-loop) | AUTHOR |
| D2 | Node-combining round 2 | Solid | Core | Medium | Moderate (catalog, parity suites, seeds) | The three finance merges DONE 2026-09-04c; payment breakdown 2×2 PROMOTED (author: in, `backlog.md` § Finance); paired-list aggregate still AUTHOR (wait) |
| D3 | `rules.md` ARR pass | Strong | Core | Low | Contained (docs) | IN (author-present) |
| D4 | Error UX on restriction violation | Solid | Core | Low | Contained | AUTHOR 2026-09-04c: no big change to the error surface; incremental improvements at the lead's discretion (the socket keeps carrying the SolError; a light input-row mark is allowed if it stays within the existing Error-values treatment) |
| D5 | Expression `/` mints no pure ratio | Weak | Adjacent | Low | Contained | DROP (leave as is) |
| D6 | INDEX: cube output + range forms | Weak | Adjacent | Medium | Contained | HOLD |
| D7 | FC A4 tails | Solid | Core | Medium | Moderate (`trigMode.ts`, `fcControls.tsx`) | Cube popup FC controls DEFERRED (author 2026-09-04c: questioning whether cube columns should be typed at all — settle that first; `deferrals.md`); per-cell mixed-unit trig IN (author: doable, small) |
| D8 | Per-card CSS conversion, step 2 | Solid | Adjacent | Medium | Moderate (`NodeSocket`, socketBox12) | DEFERRED (author 2026-09-04c: "don't touch sockets, otherwise defer"; `deferrals.md`) |
| D9 | AI palette re-enable + verification tail | Strong | Core | Low | Contained (flag + slides) | DEFERRED (author 2026-09-06: later; `deferrals.md`) |
| D10 | Document-level FC defaults | Solid | Core | Medium | Moderate (format pipeline) | DEFERRED (author 2026-09-06; `deferrals.md`) |
| D11 | Distribution accuracy widening | Weak | Core | Low | Contained | HOLD (stands, author 2026-09-06) |
| D12 | Weighted pick on Shuffle / Take | Weak | Core | Low | Contained | Shuffle DONE (`weights` socket, `list.ts`); Take has none — CLOSED as is (author 2026-09-06) |
| D13 | Decision sensitivity, minimal shape (`v2.0/10` pulled forward) | Solid | Core | Low | Contained (`DecisionSensitivityNode`) | DONE as the `DecisionSensitivityNode` (scores + scenarios frame); the perturb ±N% auto-scenarios affordance was not built — CLOSED (author 2026-09-06) |
| E1 | Composite aliasing / hidden-port promotion UI | Strong | Core | Medium | Moderate (drill-in overlay, port model) | DEFERRED (author 2026-09-06; `deferrals.md`) |
| E2 | compositeToolbarReroute — audit and close | Solid | Core | Low | Contained | CLOSED (author 2026-09-06); the logged limitation landed 11bdd8af (docked FCs recenter in the drill-in) |
| E3 | Materials & Mechanical pack content | Solid | Adjacent | Medium | Contained (a pack file) | DEFERRED (author 2026-09-06: later; `deferrals.md`) |
| E4 | Pack variant-switch socket reconcile | Weak | Adjacent | Medium | Moderate | HOLD (stands; premise corrected 2026-09-06 — op cards already reshape, packs don't) |
| F1 | Obstacle-avoiding cable shape ("routed") | Strong | Adjacent | Medium | Contained (one shape entry) | DEFERRED (author 2026-09-06: later; `deferrals.md`) |
| F2 | Traveling-cable flow pulse as meaning | Solid | Adjacent | Low | Contained (`cableFlowStore`) | DEFERRED (author 2026-09-06; `deferrals.md`) |
| F3 | Moveable / hideable toolbar chrome; UI-scale toggle | Weak | Peripheral | Medium | Wide (`layout-chrome.md`) | DEFERRED (author 2026-09-06; `deferrals.md` § Parked features) |
| F4 | Top-bar art slot | Weak | Peripheral | Low | Contained | DEFERRED (author 2026-09-06; `deferrals.md`) |
| F5 | Memory heap-snapshot investigation | Strong | Core | Low | Contained (no code until findings) | DONE 2026-09-04c (findings in the digest: no leak; dev-build bytes; `scripts/heap-probe.mjs`) |
| F6 | Grid system; cable collision avoidance (old spec); choppy zoom band | — | — | — | — | SKIPPED (author 2026-09-06) |
| G | Release tail (What's New, notes, out-of-scope ratification, tag) | — | — | — | — | IN |

---

## Track A — What-if surgery on the canvas

The competitor dive (2026-08-18) found four canvas verbs every mature node tool has and
Solenoid lacks: pin (n8n / KNIME), mute (Blender / n8n), peek (Blender viewer), hover-cone
(marimo). Together they make the canvas a what-if bench. They share one design surface (a
node-state badge) and one engine hook (a node whose `data()` is replaced by a held value or
a pass-through), so they ship as a track, badge pass first.

### A0 — Node-state badge design pass (new; prerequisite)

**Why.** Pinned, muted, stale (`compositeStaleStore`), held-after-Solve, and disarmed-sink
are all "this card is not computing what it looks like it computes" states. Today the card
has a corner badge and lock glyph (`NodeCard.tsx`, `nodeCard.css`) and the composite status
dot. Four new states without one design read as four inventions.
**Do.** One DESIGN.md § Cards addition: a single badge slot with a fixed vocabulary (pin,
mute, stale, held, disarmed), Quiet Accent Rule (state is a glyph and a border treatment,
never a fill), the tooltip copy per state (§7 Voice), and how a muted/pinned card's value
box reads (pinned shows the held value with the pin glyph; muted shows the passed-through
input). Also settle whether the badge is a click target (pin/unpin, mute/unmute) — recommend
yes, mirrored in the node context menu and a single-key shortcut.
**Gate.** Author eyeball on the dev server. **Done.** DESIGN.md section + `nodeCard.css`
vocabulary in place; A1/A2 build on it.

### A1 — Pin a node's output

**Score.** Strong · Core · Medium · Moderate.
**Grounding.** The compute pass is `graphCompute.ts` (`computeAll`, `invalidate`); every
`data()` is wrapped at `nodecreated` by `coerceInputs` then `installErrorGuards`. A held
value is a third wrapper layer, or a guard-level short-circuit: if the node is pinned,
return the held result and skip `data()`. Live connections already hold `cachedResult`
serving synchronously; volatile nodes freeze per recalc generation
(rules freezeVolatilePerCalc). Provenance: a pinned value IS a labeled literal — the origin
stamp (`errorsKeepOrigin`) should say "pinned at <time>".
**Design calls (recommend).** (1) Pin captures the CURRENT output value(s) and persists them
in the save: a pinned FrameRef must be collected to a `FrameValue` at pin time (a lazy
handle does not survive a session) — cap it (head-N with a "pin collected N rows" badge
tooltip, or refuse to pin a frame past a row budget). (2) Upstream edits do not recompute
the pinned node or its downstream cone (`invalidate` stops at pinned nodes — the one real
engine change; targetedEqualsFull must still hold: pin state is part of the graph state
both passes read). (3) Unpin recomputes. (4) A pinned node reads visually per A0.
**Steps.** Pin state in a node-keyed store (`registerNodeForget`, rules storesRegisterForget)
persisted through the text-form sidecar (rules saveViaTextForm — add to BOTH `writeTextForm`
and `readTextForm`) → guard short-circuit → `invalidate` stops at pins → card badge +
context-menu verb + shortcut → Problems panel ignores pinned errors? (no: a pinned error is
still an error). Composite internals: pins inside a drill-in persist with the composite.
**Tests.** `graphCompute` targeted-vs-full parity with a pin in the cone; text-form
round-trip of a pinned scalar / list / frame; unpin recomputes; seeds unaffected.
**Interacts with.** Calc modes (manual mode + pins are orthogonal), the parked #23 compute
cache (a pin is a user-chosen cache entry; #23 would be the automatic one — design pin's
storage so #23 can reuse it), provenance Tier 2.
**Done.** Pin/unpin from badge, menu and key; survives save/load; downstream holds; tests.

### A2 — Mute / bypass a node

**Score.** Solid · Core · Medium · Moderate.
**Grounding.** Blender's M-mute passes the first compatible input to the output. Solenoid's
socket lattice (`sockets.ts` `accepts`) decides what "compatible" means, and rules
`widenNeverNarrow` / `noAutoCross` bound it: a mute may only pass a value whose type the
output socket accepts.
**Design (the real work).** Per node, the pass-through rule: for each output, the first input
whose declared type is accepted by that output (same family, rank ≤). Nodes with no such pair
(sources, sinks, most frame verbs with a frame in and frame out DO qualify; Aggregate list→
number does NOT) show mute as unavailable — the badge tooltip says why. Multi-output nodes
map per output. Under mute the node's `data()` is skipped (guard-level, same layer as A1);
the card shows the pass-through per A0; the value box shows the passed value.
**Steps.** `mutePassthrough(node): Record<outputKey, inputKey> | null` as a pure function
over the node's sockets (testable without an editor) → guard short-circuit → badge/menu/key
→ sidecar persistence (same discipline as A1) → drill-in parity.
**Tests.** The pass-through table over every catalog leaf (a shape guard: count of mutable
nodes pinned, so a new node with a surprising rule is a deliberate change); targeted-vs-full;
round-trip.
**Done.** Mute/unmute on any eligible node; ineligible nodes explain; persists; tests.

### A3 — Peek any socket — PROMOTED (author 2026-09-04c), reshaped

The author's shape: NOT a context-menu verb that mints a Display node. Hovering ANY output
socket, after a short dwell, pops a scaled-down Display of that socket's live value — the
existing frame-hint hover (`frameHint.ts`, `FrameHintLayer.tsx`, opened from `NodeSocket`)
generalized from "an example frame for an unwired frame input" to "the value on any socket".
The line is in `backlog.md`; the earlier menu-verb design is superseded.

### A4 — Dependency-cone hover brush

**Score.** Solid · Core · Low · Contained.
**Grounding.** Isolate already computes upstream/downstream cones (`isolate.ts`,
`isolateBoundary.ts`); where-used highlight shipped; cable hover state lives in
`cableState.ts`; `FlowCableEdge` styles from it.
**Do.** On card hover (desktop pointer only; touch has no hover) with a short dwell, soft-
highlight the node's upstream cone one way and downstream the other (two treatments, DESIGN.md
Quiet Accent — a dim of everything else beats a glow of the cone), cleared on leave. Reuse the
cone walk; render through the existing selection/hover styling channels, never a new layer.
Setting-gated (Settings ▸ Canvas), default on. Must not fight the HIC gesture layer (brush
only when the DOM surface is idle).
**Tests.** Cone membership is already pinned by the isolate tests; add the dwell store's
pure state transitions. **Done.** Hover shows the cone; no jank at 200 nodes (probe script).

### A5 — Fusion indicator

**Score.** Weak · Adjacent · Low · Contained. The lazy-handle engine work landed; the seam
knows which chain flushed as one `applyMany` (`frameBackend.ts` `flushRef`) and which
consumer forced a materialization. Show it in the `CableInspector` (a "fused with N upstream
verbs / materialized here" line) and as a tiny glyph on verb cards in a debug Setting. Value
is inspection of the engine seam, mostly for the author. PULLABLE; do it in the same session
as anything touching `flushRef`.

### A6 — Optimize run mode (new; replaces "constrained optimizer node")

**Score.** Strong · Core · High · Moderate.
**Why this shape.** The deferral framed Solver parity as a sibling node of Equation. The
Solenoid-native shape is already in the codebase: **Goal Seek is a composite RUN MODE** (as
are Scenarios, Data Table, Monte Carlo, Simulation). Excel's Solver is Goal Seek generalized:
N decision inputs, one objective output (min / max / target), constraints as logical outputs
that must hold. So: a sixth run mode, **Optimize**, on the composite, with the same arm-and-
run status dot and `compositeStaleStore` semantics. No new node, no acausal machinery.
**Design.** Decision variables = the composite's chosen numeric inputs with bounds; objective
= one numeric output + min/max/target; constraints = any logical outputs (must be TRUE) and
optionally numeric outputs with ≤/≥ bounds. Method: derivative-free (Nelder–Mead with a
penalty for constraints, or COBYLA) — the subgraph is a black box, so gradient methods are
finite-difference anyway; Excel's GRG/Evolutionary are the parity reference, exact
algorithm match is not (consistencyOverQuirks). Integer/binary constraints: v1 no (say so).
Outputs: the optimum, the decision values, iterations, a converged flag, and a Problems
entry when infeasible.
**Steps.** Pure optimizer kernel in a rete-free module (rules implReteFree) pinned against
scipy on the classic test functions → the run-mode driver reusing Goal Seek's parameter
surface (`maxIterations` / `tolerance` / bounds) → run controls UI in the drill-in panel →
a seed ("Production plan" — the textbook LP-ish example, solved by penalty).
**Gate.** Author call on scope (constraint forms, integer no). **Done.** The composite
answers "max profit subject to capacity" without a Solver node; seed + kernel tests.

---

## Track B — Records and tables, finished

Standing constraint (decisions oneRecordNode): every view lands ON the one Record node as an
op or an options key; figures are display-only (no bar or card editing that would edit the
frame). Everything below was verified absent from the code in the 2026-08-18 sweeps.
**Not steals, settled then (don't re-ask):** drag-and-drop card layout editing (the layout
is TEXT by design — wireable, Note-authorable), in-view search/sort/filter (upstream verb
nodes are the answer), form entry as a separate program (the Form view shipped), linked-
record columns (Join / XLOOKUP carry the semantics), row tinting (that is conditional
formatting, `v2.0/23-conditional-formatting.md`).

### B1 — Record views polish tranche

**Score.** Solid · Adjacent · Medium · Moderate. All in `nodes/visual.ts` (`RecordOp`,
the Record node) + `RecordNode.tsx` + `RecordLayoutField.tsx` + `recordNav.ts`; the layout is
TEXT by design (wireable, Note-authorable) — every lift is an options key or a layout-text
marker, never a drag editor.
Grouped by mechanism so one session lands a coherent set:
- **Layout-text markers** (one parser change, `RecordLayoutField`): **title row** (one field
  label-less and prominent — a `#field` marker), **cover image** (a `!field` marker, drawn
  full-bleed at the card top, `object-fit` cover/contain as an options key; reuses
  `recordImageSrc`'s string-cell detection).
- **Options keys** (no parser change): **card size preset** S/M/L (scales the gallery track
  band — exactly three, like Notion/Airtable), **hide-empty** (skip null boxes on gallery and
  board cards; the card view keeps its fixed layout), **wrap/clamp** (line-clamp long values
  on tiles; the popup shows the whole card).
- **New op: List view** (a fourth `RecordOp`: one line per record, title field + trailing
  fields) — the densest browse between card and gallery; fits oneRecordNode exactly.
- **Gallery grouping + lane summaries**: a group-by column renders labeled sections inside
  the gallery (the board's lanes turned horizontal); per-lane / per-section **count + one
  aggregated column** (sum/avg of a named column) in the header — display-only, and a place
  to exceed the genre (Airtable's kanban lacks it). **Collapsible lanes** ride along. Lane
  drag-reorder and card drag stay OUT (they would edit the frame).
**Tests.** The layout-text parser (pure) for the two markers; op/option round-trip through
the text form; `seeds.test.ts` on the Records seed. **Author eyeball** for every visual.
**Done.** The seven lifts on the one node; Function Reference + Inspector copy updated.

### B2 — Select / categorical columns

**Score.** Solid · Core · Medium · Moderate (sugar) — Wide if it becomes a column type.
**The author's 2026-08-18 verdict:** "potential there for sure but needs a larger 1.4 look."
The look, in two halves that do not depend on each other:
- **FUNCTIONAL half (build first, no color):** constrained entry — a `datalist` of the
  column's distinct values on grid and Form-view string inputs in `TablePopup`. Cheap,
  arguably the better lift, no design gate.
- **DISPLAY half:** per-value tinted chips for a string column's distinct values — an opt-in
  "Chip" entry in the popup's per-column format row (`fcControls.tsx`), so the color stays
  user-authored (Quiet Accent Rule); hues from the categorical chart palette by first
  appearance; the annotation lives in `frameFormatStore` (display only, never the unit).
  The SAME mechanism is the Record node's "color-by field" and half of conditional
  formatting's palette story — build it once here so CF (`v2.0/23`) inherits it.
- **The larger question (AUTHOR):** a real enum column semantic (a `FrameColType`) or
  display + entry sugar on string columns? Recommend **sugar for 1.4**: an enum type touches
  both FrameBackends, the cargo corpus, CSV, coercion, socket coloring and the popup editor
  (the exact blast radius that parked "image as a FrameColType"); sugar delivers the visible
  value and leaves the type question to 2.0 where CF and the transpiler (Excel data
  validation lists) give it a second customer.
**Tests.** Distinct-value list is pure (`frameVerbs` distinct); chip assignment is pure and
stable under row reorder; format-row round-trip. **Done.** Both halves on string columns.

### B3 — Table popup and record popup lifts

**Score.** Solid · Adjacent · Low–Medium · Contained–Moderate. All in `TablePopup.tsx`
(1.2k lines — read the sort/edit/footer sections first) and the Record popup path.
- **Frozen header + optional first-column freeze** — sticky positioning on the existing
  `<table>` (`position: sticky` on `th` and the first `td`); no virtualization implied.
  Check: sort order, an open edit scrolling out, Copy CSV / Export staying whole-dataset.
- **Record navigation in the popup** — the card popup carries prev/next (and ←/→) so records
  flip without closing; `recordNav.ts` already holds the stepping logic for the card.
- **Image lightbox** — click an image box in any record surface → a full-size overlay with
  prev/next through the record's images or the gallery's cards (a bespoke overlay in
  `HudStack` terms: its own component, own `registerChrome`). Today clicks do nothing.
- **Chip hover preview** — hovering a value chip shows a transient mini-preview before the
  popup (the frame hint layer, `FrameHintLayer.tsx`, is the render precedent).
**Gate.** `layout-chrome.md` row for the lightbox; DESIGN.md for the sticky treatment.
**Done.** Four lifts, author-eyeballed.

### B4 — Computed Column UX tail

**Score.** Solid · Core · Medium · Moderate. Ratified 2026-07-29, residual UX only:
- **Shared column-picker component** — Sort / Get Column / Join name columns as free text
  today; one picker fed by the static frame shape (`frameShapeResolver.ts` — the resolver
  walks forward from literal sources, so a picker can list columns before data flows) with
  free-text fallback when the shape is unknown. One component, three first customers.
- **Output-column format / unit controls on the CC node's card** (the popup half shipped).
- **λ view-as on the card** (the inline lambda's declared return kind).
**Tests.** Picker is pure over `FrameShape`; existing computed-column suites stay green.
**Done.** The three customers use the picker; CC card carries format/unit.

### B5 — XMATCH / XLOOKUP remainder

**Score.** Weak · Core · Medium · Contained. Settled 2026-08-11 except: a 1×N / N×1 MATRIX
lookup VALUE still `#SHAPE!`s, and per-argument spill for other functions
(`wholeArrayArgs` / `prepByShape`) awaits the general mechanism. Both are in the matrix gate
of `excelFormula.ts` and the shared `xmatchIndex` kernel. PULLABLE; do the matrix-value case
alone (orientation-free 1-D matrices are already accepted as the lookup ARRAY — mirror that
for the VALUE). Do not reopen "XLOOKUP takes a frame" (settled: Get Column → XMATCH).

### B6 — Peek dock (AUTHOR)

Notion's side peek: value/figure popups docked right instead of center-overlay, canvas stays
interactive; the Inspector dock (`sol-inspector-docked`) and docked Report are the shipped
precedents and `layout-chrome.md` already reserves the right-side dock column. Per-surface
default like Notion (gallery → center, table → side) if it lands. Solid · Adjacent · Medium ·
Moderate. Needs the author to want a third docked panel; if yes, it is one `layout-chrome.md`
row and a popup-host option, and B3's record navigation gets much better in a dock.

### B7 — Gallery tile click → picks the row (AUTHOR)

Grist's linked card-list: highlight the clicked tile and drive the card view's row from it.
The `picked` row OUTPUT shipped and was removed 2026-08-19, so this needs a channel that is
not a data output socket — a figure gaining an input gesture is an author call (it is the
first figure that would carry per-viewer state into the document). Recommend: a display-
only "focused record" per Record node, persisted in the sidecar, that a second Record node
(card view) can FOLLOW by name (an options key, not a cable). Solid · Adjacent · Medium ·
Moderate.

### B8 / B9 — held

- **Timeline / Gantt + calendar figure** → `2.0-plan.md` (the author: "loves Gantt, not
  now"; a MUST eventually). The decomposition stands: display figure in the Waterfall class
  + a **Schedule** computation (forward pass over Task / Duration / Depends; WORKDAY math in
  `date.ts`) + no bar editing ever. Calendar is the same family; the ruling covers it.
- **Two-axis board (swimlanes)** — HOLD until B1's grouped gallery proves the lane machinery.
- **Color-by field** — ships as B2's display half; the Record option key is a one-liner after.
- **Table/cube popup virtualization** — HOLD (author 2026-08-30). The Path A vs B analysis
  stays in `deferrals.md`.

---

## Track C — Everyday sources

### C1 — Widget nodes Tier 1 (`v2.0/16-widget-nodes.md`)

**Score.** Strong · Core · Medium · Moderate. The bundle doc is complete and current: Weather
(Open-Meteo), Geocode, Currency/FX (Frankfurter), Holidays (Nager.Date), Time Zone Convert +
World Clock (pure `Intl`), QR Code (pure). Each ≈ the Data Feed build: provider file + node +
component + fixture tests + seed; the headless runner gets them free.
**The four author calls (unchanged, listed in the bundle):** FX yes/no (reverses the 1.1
Data-Feed cap); the standing provider policy (Open-Meteo / Frankfurter / Nager.Date as built-
in dependencies; lat/lon leaves the machine); where they live (recommend: the fetching four as
Connections siblings, the computing two in Timesavers); the "Garden Dashboard" seed.
**Sequencing.** Geocode + Weather first (the anchor pair and the pitch case), then Holidays
(completes the WORKDAY story), TZ/QR (pure, anytime), FX last (the gated one). Ship with C2.
**Done.** Bundle exit criteria; Garden Dashboard seed; What's-New slide candidate.

### C2 — Per-document network permission (new)

**Score.** Solid · Core · Low · Moderate.
**Why now.** Widget nodes multiply the sources that fetch on open, and Weather sends a
location. Today a document's connection nodes fetch as soon as the graph loads. Sinks already
load DISARMED (rules sinkRunButtonOnly) — sources should get the mirror for documents you did
not author: **a document opened from a file or import fetches nothing until you allow it**
(one notice: "This document connects to 3 services — allow?"), remembered per document in
the sidecar (`meta`), never for your own autosaved documents. This is the first slice of the
2.0 "trust on open" story (`v2.0/21-collaboration.md`) and stands alone.
**Where.** `connectionStore.ts` (a doc-level gate in front of the background fetch),
`persistence.ts` load path (foreign-vs-own is known at import), the notice channel
(`noticeStore`), Settings ▸ Data (a "always allow" escape hatch).
**Tests.** `connectionStore.test.ts`: gated doc fetches nothing; allow releases all; own
docs unaffected. **Done.** Foreign documents are quiet until allowed.

### C3 — Data Feed widening — HOLD

Weak · Adjacent · Medium · Contained. A real symbol-search picker + more providers stays
Excel STOCKHISTORY scope (no crypto/FX/real-time/options/fundamentals). Nothing pulls it
now; Alpha Vantage keyed + FRED keyless cover the story. Reopen on a user ask.

### C4 — iFrame / embed node — AUTHOR (lean DROP)

Weak · Peripheral · Medium · Moderate. The gate call is CSP `https:` vs an allowlist, and the
non-negotiables are written (sandbox without top-navigation, no-referrer, https-only, click-
to-load, capped concurrency). It is the one node that renders someone else's live page inside
a document — with 2.0's shared documents that becomes a phishing surface. Recommend the
author rule it OUT in the out-of-scope ratification unless a concrete use survives.

### C5 — Timesaver date idioms needing config

Solid · Core · Low · Contained. Fiscal Quarter (start month), Age (DATEDIF "MD" nuance), Nth
Weekday — each is a small config socket or dropdown on a `packs/timesavers.ts` preset (the
zero-config Quarter / Days-in-Month already shipped). Judgment call per idiom is the
author's; the build is routine. IN.

---

## Track D — Surface hardening

### D1 — Formula surface allowlist, Option A (IN) and Option B (AUTHOR)

The written proposal is `docs/plans/formula-surface-allowlist.md`. **Option A is DROPPED**
(author's call via Han, 2026-09-04c): its step 0 found that of 174 undeclared names, 127 today
broadcast a CLEAN element-wise array over a list, so the "refuse undeclared array args" guard
would regress 127 correct functions (`COS({1,2,3})` …), not the "handful" it assumed — full
method + REGRESS/IMPROVE/RANGE lists in dev-notes.md (2026-09-04c). Option B (D1b, "a name
exists iff declared") is the path: an author-present per-name audit, the IMPROVE list from A's
step 0 as its starting set (verify each — the one-arg probe over-counts multi-arg financials).

### D2 — Node-combining round 2

Solid · Core · Medium · Moderate (decisions maximalMerge; after any merge: `seeds.test.ts`,
`nodeOps.test.ts`, `formulaNodeCoverage.test.ts` beside the parity/catalog suites).
- **IN — the three finance merges already SPECIFIED and held for the author's eyeball**
  (specs in git 68bf5679): discount securities (TBill / SecurityDisc / PriceDisc / PriceMat),
  ACCRINT + ACCRINTM, BondPrice + OddCoupon. The Set card (8d77cf7f) is the shape precedent.
- **AUTHOR — paired-list aggregate** (SUMPRODUCT + SUMX* + CORREL + COVARIANCE + WAVG: the
  two-list Aggregate; author: wait) and **payment breakdown 2×2** (IPMT/PPMT × CUMIPMT/
  CUMPRINC; author unfamiliar — explain first: one card, op = interest/principal, arg =
  single period vs cumulative range).

### D3 — `rules.md` ARR pass (IN, author-present)

Strong · Core · Low · Contained. The rule index and marking procedure are in place; the pass
is the author walking every `[INFERRED]` / `[DEFAULT]` rule and conferring `[ARR]` or
amending. Best done EARLY in 1.4, before the track work adds rules (A1/A2/C2 each add one).

### D4 — Error UX on restriction violation (AUTHOR)

Solid · Core · Low · Contained. Typed error out the socket vs the node flagging the offending
input locally. Recommend: BOTH, consistently — the socket carries the SolError (rules
errorInErrorOut, nothing downstream can be silently right) and the card marks the input row
whose value violated (the origin stamp already knows the input). A rule line + one shared
card treatment.

### D5 — Expression `/` mints no pure ratio — DROP

Weak. Expression is deliberately type-agnostic at its boundary (unit cells stripped); the
Divide NODE mints `5:1`. Recommend ruling it "leave" in the review and deleting the entry.

### D6 — INDEX cube output + Excel range forms — HOLD

Weak · Adjacent · Medium · Contained. Cube-typed output and `row=0`/`col=0` whole-row/col
forms; nothing pulls it. Stays parked with its `archive/cube-node-scope.md` pointer.

### D7 — FC A4 tails

- **Cube popup FC controls** — IN. `TablePopup` has the per-column format + unit row via
  `fcControls.tsx`; cubes wait on nothing now that `CubeColumn` is typed. Moderate, mostly
  wiring the same row into `CubePopup`.
- **Per-element mixed-unit trig** — PULLABLE. `resolveTrigModes` reads one socket-level unit;
  a list mixing deg/rad cells should interpret each cell in its own unit. Touches the one
  compute-time unit read; pin with a mixed-list fixture.

### D8 — Per-card CSS conversion, step 2 — PULLABLE

Solid · Adjacent · Medium · Moderate. Census done (`scripts/card-css-census.mjs`). Target
order: the socket-dot ring (2,221 elements) → pseudo-elements; corner badge / lock; section
divider. Sockets may be CSS-positioned (verified). Charts/popups do not move. Rule socketBox12
must stay green; re-run the probe scripts. Best paired with A0 (the badge redraw).

### D9 — AI palette re-enable (IN)

Strong · Core · Low · Contained. `AI_ENABLED = false` in `aiKey.ts`; the What's-New slide
and the release-notes `[slide]` entry were pulled. The parked verification tail: one real-key
end-to-end (`npm run ai-prompt`), palette on the preview (author eyeball), Tidy on an
all-at-0,0 generated graph, one desktop CSP smoke. Known + accepted: Apply drops undo
history. Later-if-wanted: streamed rendering; OAuth-style connect (which 2.0 accounts would
give for free — `v2.0/21`).

### D10 — Document-level FC defaults (AUTHOR)

Solid · Core · Medium · Moderate. Default places / number format per document — a format-
pipeline integration (`makeAnnotationResolver`'s fallback tier) carried in `SavedGraph.meta`
via the sidecar. Author-present because the resolution order (FC > column format > doc
default > type default) is a product call.

### D11 / D12 / D13

- **Distribution accuracy widening** — HOLD (representative-point validation only; widen if
  accuracy is ever in doubt).
- **Weighted pick** on Shuffle / Take (`np.random.choice(p=)`) — PULLABLE, the last open
  `python-r-gap.md` Tier-2 item; a `weights` socket on the existing cards.
- **Decision sensitivity, minimal shape** (`v2.0/10`) — PULLABLE: a "perturb ±N%" affordance
  on `DecisionSensitivityNode` that auto-generates the scenarios frame, verb unchanged. The
  Monte Carlo shape stays 2.0.

---

## Track E — Composites and packs

### E1 — Aliasing / hidden-port promotion UI (DEFERRED, author 2026-09-06)

Strong · Core · Medium · Moderate. The data model has `hidden` / `advanced` per port and baked
defaults; there is no UI to flip exposure or edit the default. Home: the drill-in overlay's
port promotion strip (`FlowCompositeOverlay.tsx`). Include the pack-shell "many ports → one
shell parameter" aliasing (one exposed port fanning to several internal inputs) — that is the
piece the composite PACK-node shape (2.0) needs, so build the model half generally.
**Tests.** Composite serialization round-trip with hidden/advanced/aliased ports;
`compositeLogic` unpack keeps aliases as real cables.

### E2 — compositeToolbarReroute: audit and close (DONE 2026-09-04c)

Audited every top-toolbar / menu-bar / mobile-bar / keyboard verb with a drill-in open (full
list + method in the 2026-09-04c dev-notes digest). All were already seam-correct except the
keyboard-less **delete button** (mobile / tablet), which reached MAIN because the drill-in
swapped selection + arrange but not delete; fixed with `swapDeleteSlot` on the existing
`swap*Slots` pattern (`canvasCommandsSwap.test.ts`). Known limitation logged: docked FCs inside
a drill-in don't recenter (`repositionDockedTo` no-op — backlog § Composites). Flagship removed
from `2.0-plan.md` and the decisions pointer.

### E3 — Materials & Mechanical pack content (PULLABLE)

Solid · Adjacent · Medium · Contained. The INTERPOLATE gate cleared 2026-07-15; what remains
is domain content (beam deflection cases, bolt torque/preload, pipe schedule + hardness
tables, stress concentration factors — `pack-composite-plans.md`, `archive/reference-packs.md`).
A pack file on the `packs/*.ts` pattern. Variant-heavy entries (Roark cases) want E4 or a
dropdown with fixed sockets (the shipped custom-node convention) — use the latter.

### E4 — Pack variant-switch reconciles the socket set — HOLD (premise corrected 2026-09-06)

Weak · Adjacent · Medium · Moderate. The hand-rolled op cards DO reshape their sockets on a
dropdown switch (`finance.ts` `reshapeInputs`, `keysDroppedBySwitch` on the Workdays /
Distribution / Series cards; cables on shared keys survive, the rest prune under
`onePrunePath`). What has no such mechanism is a declarative PACK entry (`packs/*.ts`), whose
socket list is fixed at registration. Nothing waits on that. Reopen only if E3 cannot express a
case with fixed pack sockets.

### Composite pack-node shape + pack distribution → 2.0

`pack-composite-plans.md` (the queued composites) and the B1 remainder (`archive/v1.1-plan.md`:
tiers, manifest, dependency resolver, load-time prompt) are one program; it lands with 2.0's
save-format work (`2.0-plan.md`).

---

## Track F — Canvas chrome and feel

### F1 — Obstacle-avoiding cable shape (AUTHOR: license)

Strong · Adjacent · Medium · Contained. `obstacle-router` (pure-TS libavoid port) as ONE more
entry in the cable-shape selector ("routed"), fed node boxes from `Surface.measured`; never a
replacement for `cablePaths.ts` (ribbons, conduit faces and socket angles stand on the walk
router). **Gate: LGPL-2.1.** Dynamic-linking terms do not map cleanly onto a bundled web/Tauri
build. Options for the author: (a) accept LGPL with the relinking obligation documented in
THIRD-PARTY-NOTICES and the router loaded as a separate chunk (the closest a bundle gets to
"dynamic"); (b) a clean-room orthogonal router of our own (Medium–High, the nudging is the
hard part); (c) skip. If (a), this supersedes the old cable-collision-avoidance spec
(`archive/cable-routing.md` §2) — delete that parked entry.

### F2 — Traveling-cable flow pulse as meaning (AUTHOR)

Solid · Adjacent · Low · Contained. The landing page's marching-dash rendering
(`LandingScenes.tsx` `.sol-cable__flow`) applied to app cables — allowed by DESIGN.md's
no-decoration stance only if the pulse MEANS something: recommend it marks **live data
flowing** (a connection node refreshed within its interval → its downstream cables pulse
briefly) and **a pinned/held boundary** (no pulse past a pin). `cableFlowStore` already
holds the bead toggle. Never-degrade-cables rule applies (GPU-cheap CSS animation only).

### F3 — Moveable / hideable toolbar chrome; UI-scale toggle — HOLD

Weak · Peripheral · Medium · Wide. Customization slices; the UI-scale toggle subsumes every
per-panel resize ask (do not build per-panel resize). `layout-chrome.md` makes the cost
visible. Reopen on a real ask.

### F4 — Top-bar art slot (AUTHOR)

`TopBar.tsx` holds the empty middle-gap div; needs author art. Nothing to plan.

### F5 — Memory heap-snapshot investigation (IN — no code until findings)

Strong · Core · Low · Contained. The parked "high memory for a light app" bug is the one
parked item worth a measurement now: a Chrome heap snapshot on the getting-started seed vs a
blank doc (retained node clones? HIC pyramids? popup caches? per-doc tab growth). Output: a
finding in the digest and, if a leak is found, a contained fix. The virtualization lever
(`onlyRenderVisibleElements`) is 2.0's canvas-at-scale bundle (`v2.0/22`); the snapshot tells
that bundle where the bytes actually are.

### F6 — held

Grid system (`grid-system.md`), the old cable-collision spec (superseded by F1 if adopted),
the choppy zoom band (author: reopens only on their say-so, starting at T1/T2).

---

## Track H — Allocator-family analytical nodes (RULED 2026-09-06: H1, H3, the hours seed, H6 IN; H7 WAITS; H2, H4, H5 not called)

Same DNA as the **Allocator** (renamed from Budget Allocator this session): a small closed-form
linear problem, exact and solver-free, tedious to wire by hand. From a domain sweep the author
promoted three; parked two to `deferrals.md` (blend/mix, loan-term solver); ruled break-even a
composite run mode (A6 territory), not a node. **Each ships WITH a seed** (bake it via
`scripts/tune-seeds.mjs`, per the 2026-09-03 authoring rule).

### H1 — Payoff Planner (IN, author 2026-09-06)

Nearest neighbour to the Allocator — the same "spread a fixed pot by a rule, cascade the freed
slack" mechanic, over time instead of across ranges.
**Design.** Rows are debts (Balance, APR, Min payment) + an `extra` monthly literal. Order by
**Avalanche** (highest APR) or **Snowball** (smallest balance) via ArgSelect; pay every minimum,
throw `extra` + each freed payment at the head debt, roll month by month. Closed-form
amortization, no solver. Output mode-selected: a schedule frame (Month · per-debt balance) or a
summary (Debt · Months · Interest · Payoff date). Carry the Balance column's currency unit.
**Seed.** "Debt payoff" — 3–4 debts where avalanche vs snowball visibly reorder and differ in total interest.
**Gate.** Schedule-frame vs summary as the default output (author).

### H2 — Apportionment (not called 2026-09-06; stays a proposal)

Split N indivisible units across groups by weight so they sum **exactly** to N — the integer
sibling the Allocator deliberately isn't (continuous, range-bound).
**Recommendation: a NEW node, not an Allocator mode.** The Allocator requires min/max price
columns and emits fractional money; apportionment takes a single total N + weights, no ranges,
integer output, exact-sum. Folding it in would fork the card's socket set and validation — here
noDuplicateNodes points the other way (different inputs, different invariant). Reuse the
water-filling intuition, not the node.
**Design.** Weights column (or wired list) + total N literal; method ArgSelect — **Largest
remainder (Hamilton)** / **D'Hondt** / **Sainte-Laguë**. Output: Group · Quota · Seats, integer
and exact-sum.
**Seed.** "Seats by population" — the classic Hamilton vs D'Hondt divergence on one dataset.
**Gate.** Confirm new-node over mode; which methods v1.

### H3 — Group Cost Settle (IN, author 2026-09-06)

Rows are people (Paid, optional Share weight); output the minimum set of transfers that settles
everyone up. Net each balance, greedily match the biggest creditor to the biggest debtor.
Linear, greedy-exact, a real pain by hand.
**Design.** People frame in; net = Paid − Share·(ΣPaid / ΣShare); emit a transfers frame
(From · To · Amount) + a per-person Net column. Currency unit carried.
**Seed.** "Trip split" — four people, uneven payments, settled in three transfers.
**Gate.** Equal-split vs Share-weighted default (both supported; pick the default).

### Scheduling slice — calendar-shaped, solver-free (PROPOSALS 2026-09-04b, AUTHOR-GATED)

The first pass (Hours Balancer as an Allocator mode, Shift Assignment via Hungarian, a
round-robin Rotation generator, an Erlang staffing requirement) was judged decent but not
calendar/schedule-shaped enough. Reworked here for a **simplistic user** — someone with a
roster, a date range and a total to spread — keeping only what has an exact, explainable answer
with **no solver**. Each ships with a seed. Nothing below starts until the author picks.

**Not a node — hours balancing IS the Allocator (the seed is IN, author 2026-09-06).** "Spread 160 shift-hours across a team with
contract min/max and a weight" is Fit budget with an hours unit. Propose a **seed** ("Balance a
team's hours": people rows Min · Max · Weight, the Allocator with `h` on the Min column, a Display
of Share as %) and loosening the Allocator's socket copy from "price range" to "range" so hours
read naturally. Zero code beyond copy.

#### H4 — Rota (shift fill-in) — not called 2026-09-06; stays a proposal
The direct answer to "who covers which shift". **In:** a Shifts frame (Date · Shift · Needed,
optional Hours) and a People frame (Name, optional Max hours, optional Off — a text cell of days
or dates the person can't work). **Mode (ArgSelect):** *Round robin* — everyone in turn in roster
order, skipping the unavailable, resuming where it left off; *Fewest hours first* — each slot goes
to the available person with the least assigned hours so far (ties by roster order). **Out:** an
Assignments frame (Date · Shift · Person) and a Load frame (Person · Shifts · Hours); a slot no
one can take stays as an explicit `Unfilled` row rather than silently vanishing. Greedy,
deterministic, explainable — not optimal, and says so. A "no back-to-back" or skills constraint
tips into the Optimize run mode (A6); v1 stays pure.
**Seed.** "Weekend cover" — 6 people, 4 weekends, two slots a day, two people with days off; the
Load table shows the fairness.  **Gate.** Confirm the two modes; whether Off is days-of-week,
dates, or both.

#### H5 — Spread over dates (phasing) — not called 2026-09-06; stays a proposal
The Allocator's proportional DNA over a calendar: one total, one date range, one rule. **In:**
Total, Start, End (or Days), a Holidays date list. **Rule (ArgSelect):** *Every day* / *Working
days* (skip weekends + holidays) / *By weekday weights* (a Mon..Sun weights row) / *Ramp* (front-
or back-loaded, linear) / *By month length*. **Out:** Date · Amount · Cumulative, unit carried
from Total. What Excel users build from WORKDAY/NETWORKDAYS by hand. Simple user: phase a
quarterly budget, a savings plan, planned hours per day.
**Seed.** "Phase a budget across Q4" — even vs working-days vs ramp side by side, one chart.
**Gate.** Which rules v1; whether a wired per-date weights frame replaces the weekday row.

#### H6 — Schedule (task timeline, CPM forward/backward pass) — IN, author 2026-09-06 ("the big one")
**In:** a Tasks frame or cube — `Task` (first text column; names must be unique, a duplicate
is `#VALUE!`), `Duration` (first number column; working days, 0 = a milestone), `Predecessors`
(a text cell `"A, B"` of task names, or a list cell in a cube — the shape a TaskNotes
`blockedBy` arrives in; blank = none; finish-to-start only, no lags in v1) — plus
a project `Start` date, a `Working days` switch (on: skip weekends via the existing
`weekendSet`/`holidaySet` helpers in `nodes/date.ts`, the WORKDAY relation; off: calendar
days) and an optional `Holidays` date list. **Out:** the input rows in their original order (a cube in → the same cube,
nested cells untouched) with `Start · Finish · Float · Critical` appended (Start = earliest start, Finish = Start + Duration
in the chosen calendar, Float = latest start − earliest start in days, Critical = Float is 0);
`Project finish` as a scalar output beside the frame, and **`gantt`** — the schedule as Mermaid
`gantt` source (one `section` per project if a Project column exists, `crit` on critical tasks,
`after` edges from predecessors) — a string the Mermaid node draws on the canvas and Write to
Obsidian emits as a fence Obsidian renders natively (`v2.0/24-obsidian-vault.md` F1); the 2.0
Gantt figure is the interactive canvas sibling, not a prerequisite. **Algorithm:** Kahn topological order over
the predecessor edges; forward pass (earliest start = max of predecessors' finish, or `Start`),
backward pass from the project finish (latest finish = min of successors' latest start); all
exact integer day arithmetic on serials — no solver. **Errors:** a cycle, an unknown predecessor
name, or a negative/non-finite duration → one `#VALUE!` for the frame with the offending task
named; a null duration reads as 0 (a milestone), never as an error. **Class:** an eager
`FrameValue → FrameValue` verb like `decisionMatrix` (materialize, compute in JS, re-emit) —
NOT a lazy `FrameOp`, so `oneVerbCorpus` does not apply (no Polars twin; the corpus rule covers
verbs both engines speak). Unit-tested as a pure function: chain, diamond, two roots, milestone,
weekend spanning, holiday inside a task, cycle, unknown name, float on a parallel branch.
**Consumers.** The data half of the 2.0 Gantt figure; the exact input shape the TaskNotes feed
emits (`v2.0/24-obsidian-vault.md` F1: `predecessors` from `blockedBy`, Duration from
`timeEstimate` ÷ an hours-per-day literal). Simple user: "when will these be done, and which
task can't slip?"
**Seed.** "Kitchen remodel" — 8 tasks, a diamond in the middle, one holiday, the critical path
filtered out beside a Display and `Project finish` in a KPI.
**Gate.** Working-day arithmetic on by default (recommend yes); Float in v1 (recommend yes — it
is the backward pass's only product and Critical derives from it).

#### H7 — Common free time — WAITS (author 2026-09-06: until Solenoid can connect to a person's calendar; TaskNotes might help)
**In:** an Availability frame (Person · Day-or-Date · From · To, times as serials or "9:00"),
optional At least N people, Minimum minutes. **Out:** Day · From · To · Who (count or names) —
the windows the whole group (or any N) is free for at least the minimum. A sorted interval
sweep, exact. Simple user: "when can the four of us meet?"
**Seed.** "Find a meeting slot" — four calendars, one 45-minute window pops out.
**Gate.** Whether it takes busy windows (calendar exports) as well as free ones.

**Parked from the first pass** (`deferrals.md`): Shift Assignment (people × shifts score matrix
→ best one-to-one matching; Hungarian is exact but reads as a solver and answers a question a
simple user rarely asks) and Staffing requirement (Erlang C; call-centre specific).

---

## Track I — Fold the Format Controller into the Display (AUTHOR PROPOSAL 2026-09-04, LATER)

**The idea (author).** Instead of a docked FC that pushes format/unit BACKWARD onto the box
feeding it and forward through the resolver, the Display node IS the format point: a value is
formatted and unit-tagged where it is shown, and that flows DOWNSTREAM only until another
Display overrides. One direction, no docking.

**For.** Deletes the two-direction machinery — `fcDocking.ts`, `dockedNodeStore`, dock
reconcile, host-socket hiding, reposition-after-layout, the upstream resolver walk — and the
"a chip on a socket formats the box behind it" rule users must learn. The FC popup becomes the
Display's popup. Unit lock states (forwarding / lockedByConvert / dictation) stay as they are,
in fewer places.

**Against, and the answer.** The FC's one irreplaceable job is authoring a unit on a LITERAL at
its source (unit-flow lane A: an FC on a Number input). So the fold needs its second half:
**source nodes (Number, Date, Text, list inputs) carry the same control** — format and unit are
set at sources and at displays, never pushed backward; everything between inherits downstream
(the 2026-09-04 formatFlowsDownstream rule is this model's first half, already landing). Smaller
losses, accepted: formatting a compute node's OWN box means inheriting or adding a Display; a
multi-output node wants one Display per formatted output; charts / lambdas / documents keep
their view-as options on the Display.

**Scope.** Delete: the FC node, docking (`fcDocking.ts`, `dockedNodeStore.ts`, `fcReconcile.ts`'s
dock half, `FormatControllerNode.tsx`/`.css`, the socket-hiding + reposition paths, the
`downstreamAnnotation` walk). Move: the format popup onto Display + the source nodes;
`formatAnnotationStore` keyed per node. Migrate: every seed with an FC (19 in unit-flow alone —
re-author as source units / Display formats, re-tune), the unit-flow seed's captions (A–J),
`format-model.md`, `subsystem-invariants.md` § Unit flow, `rules.md` (`unitOnValue`,
`relaysTransparent`, `formatFlowsDownstream`), the node-coverage FC entry, the Reference copy.
**Sequence.** After the downstream-flow + surfaces work has been lived with; then a 1.4 item
with its seed migration in the same commit series. **Gate.** The author's go, and the source-node
control design (a compact unit/format chip on the input cards — DESIGN.md pass).

## Track G — Release tail

- Reset `release-notes-features.md` to the 1.4 list as items land (the 1.3 list is at the
  v1.3.0 tag); What's-New slides for the headliners (A-track, C1, D9 if re-enabled).
- The out-of-scope ratification (DRAFT since July, no ARR) — §3/§11/test 3 already read the
  author's collaboration order; the rest still awaits the author's word.
- Version bump + merge `develop` → `main`; the tag is the author's (CLAUDE.md).

## Author calls this plan needs (consolidated)

1. **Ratify the cut** — the IN column; promote PULLABLEs by taste.
2. **A0 badge design** (eyeball) and **A6 Optimize** scope (constraint forms; integer no).
3. **B2** sugar vs enum type (recommend sugar); **B6** peek dock yes/no; **B7** focused record.
4. **C1** the four widget-node calls (FX, provider policy, homes, seed).
5. **C4** embed node: rule out?
6. **D1b** the allowlist audit session; **D2** the two unfamiliar merges; **D4** restriction
   error UX; **D10** doc-level FC defaults resolution order.
7. **F1** LGPL stance; **F2** pulse meaning; **F4** art.
8. **D3** the ARR pass session (early).

## Gaps this plan fills (new components, listed once)

- **A0 node-state badge vocabulary** — one design for pin / mute / stale / held / disarmed.
- **A6 Optimize run mode** — Solver parity as a composite run mode, not a node.
- **C2 per-document network permission** — sources load quiet on foreign documents; the
  first slice of 2.0's trust-on-open.
- **B4 shared column picker over the static frame shape** — the first UI consumer of
  `frameShapeResolver.ts` beyond the cable inspector (Bet 3's payoff starts here).
- **B2 chip mechanism as a shared primitive** — categorical color built once for Record
  color-by, the popup, and conditional formatting.
- **E1 general port aliasing** — the model half the composite pack-node shape needs.
