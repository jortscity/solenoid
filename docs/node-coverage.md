# Solenoid — Node coverage

`nodeCatalog.ts` is the source of truth — the Add menu AND the in-app Function
Reference (Ctrl+/) are generated from it, so a node's catalog `description` is its
Reference entry. This file is a human-readable inventory by category plus the
non-obvious per-node notes. (Moved out of CLAUDE.md to keep the always-loaded doc
lean; update it when the catalog changes meaningfully.)

**Multi-op declarations (`nodeOps.ts`)** — one declaration per OP family (a node class with an `op` field). `expose` is the MENU axis: `"collapsed"` (default) gives one Add-menu leaf with every op reachable from SEARCH ("Chart: Column"); `"leaves"` generates a leaf per op — a family earns per-op leaves deliberately, the default keeps dropdowns from spraying the tree. The `{ }` marker is DERIVED, never declared (shown iff the node has ops with no leaf of their own; `mark: false` suppresses it for labels that already enumerate, e.g. "GCD / LCM"). There is no semantic axis: a family is in `nodeOps.ts` because its `op` field's values are ops; an ARGUMENT (a sort order, an aggregator, a view) is stored under its own name, picked with `ArgSelect`/`SegToggle`, and is not declared (rules opArgDistinct; `DESIGN.md` § Op pickers).

**Add-menu pane ROW BUDGET** — the rule behind every catalog regrouping: a category
pane holds about **12 rows** before it needs a scrollbar, so a pane growing past ~12
gets its tail folded into subcategories rather than left flat (the 2026-07-16 sweep
applied this across charts, statistics, hyperbolics, bitwise, and the frame verbs;
control widgets folded into Input 2026-07-09, freeing the top-level row Packs uses).
A future node addition that flattens a pane back past the budget reintroduces the
scrollbar that sweep removed.

**Scalar**: Input, Constant, Arithmetic (all ops), Math functions, Rounding, Trig (a `Math` node's trig ops carry a **deg/rad/Auto** SegToggle — Auto (default) reads the incoming unit so a °-tagged value computes in degrees, else radians = Excel parity; Rad/Deg pin it; forward trig converts the input, inverse trig converts the result and tags the `deg` unit; resolved at recompute time by `trigMode.ts` — the one place compute reads units), Hyperbolic, Logarithms, Combinatorics, Bitwise, Engineering, **Comparison + Boolean logic** (emit the first-class **logical** type — purple, TRUE/FALSE, coerces ↔ 1/0; AND/OR/NOT are Kleene 3-valued on `null`. **Restructured 2026-06-23:** the old multi-op `LogicalNode` was split into **`BooleanOpNode`** — AND/OR/XOR/NAND/NOR/XNOR as **N-ary reducers** over extensible operand rows, all emitting logical so the op switch never swaps the socket — plus a standalone **`NotNode`** (unary element-wise flip). **Operand inputs are `logicalcombo` (purple), not `numlist` (2026-07-15)** — a boolean op takes booleans; a number still bridges in (0/1 ⟷ FALSE/TRUE). Rows are WIRE-ONLY like IfNode's condition (you wire a comparison/test) and a standalone **`IfNode`** (value passthrough, `util`-colored like its Choose/Switch/Ifs siblings, NOT logical)), **IF / IFS / SWITCH / CHOOSE** (the value selectors — all **variadic now**: CHOOSE has add/remove value rows over a fixed `index`; IFS takes any number of condition/value pairs **plus an "Otherwise" fallback**; SWITCH takes any number of when/then pairs between a fixed `expr`/`default`; see `docs/archive/node-arity-audit.md` + the **Labeled-slots vs list-input rule** below for why these are labeled per-slot inputs, not list sockets), IFError, NA, **Type Check** (`IsTestNode` — labeled "Type Check"; op dropdown ISNUMBER/**ISNULL**/ISBLANK/ISERROR/ISNA/ISLOGICAL/ISTEXT/ISNONTEXT — the `islogical` op value is unchanged for save compat (the label is ISLOGICAL, the real Excel name; NAME-4). ISNULL is the per-cell missing test (≠ unwired); every check tests per-cell to any depth incl. a frame. Doubles as the error-inspection surface, see Error values), **ISEVEN/ISODD** (`IsEvenOddNode` — a single parity-test node with an even/odd toggle; emits the **logical** type TRUE/FALSE (not 1/0) and broadcasts over a list — extracted from the Math op-dropdown 2026-06-23 so it could carry a logical socket; lives in the Logic menu), **Alert** (watch a value — fires a toast + logs to the Alerts HUD on a changed trigger; see subsystem-invariants "Alert node + Alerts HUD"). **Between** (inclusive Low ≤ Value ≤ High) and **Is Close** (|A−B| ≤ tolerance) — numListIn predicates broadcasting like Comparison, logicalcombo out (2026-08-23). *(Removed 2026-06-23: the **T / N / TYPE** coercion-info nodes — their input socket was never rendered so they were unwireable, and Cast + the socket type system + Test supersede them; and the multi-op **`LogicalNode`**, split as above. Old saves load these as Placeholders — wiring/data kept, re-saved as the original type.)*

**Lists**: Literal, **Series** (ONE list-generator node, nodeCombiningRound1: Range / SEQUENCE / LinSpace / Geometric / Fibonacci / Repeat as op-selected parameterizations; Start shared so switches keep the cable — the standalone Geometric/Fibonacci/Repeat nodes were folded in 2026-08-24), Aggregate (a fixed-op 1-D list reducer — 20 ops: sum/avg/min/max/count/countdistinct/median/product/stdev/var/geomean/…; class `AggregateNode`, header title "Aggregate". Renamed from "Reduce" so it isn't confused with the table-taking REDUCE lambda), List Filter (filterOneJob: 1-D ONLY; label renamed from FILTER 2026-07-09 with its Frame sibling, the list against its own values — the frame Filter's condition engine, extensible AND/OR op+value rows, text ops + Match case, `anylist`; the old mask and table acceptance are GONE — tables go to Frame Filter, which a matrix enters as auto-named Col1..N; permanent `Dropped` = the exhaustive complement), Fill (a bundled missing-value node — `FillNode`, op selector: constant/ffill/bfill/mean/median/mode/interpolate/drop/coalesce; per-cell errors pass through, statistics impute from present values), List Sort, Reverse, Slice (Take/Drop is now the one rank-preserving `TakeDropNode` under 2-D Tables ▸ Select — list, matrix or scalar in, same rank out, D6) (Reverse/Slice/Shuffle/NthElement/Interleave/Pad are element-agnostic `anylist` — position-only ops, the appendLadder coherence sweep; the single-input ones — Reverse/Slice/Shuffle/NthElement/Pad, + matrix TRANSPOSE/CHOOSEROWS/COLS/TAKE-DROP/EXPAND — are PASSTHROUGH nodes (2026-07-15): adoptive in+out + a `passthrough()` decl, so a reversed/transposed date list keeps its type + date formatting downstream instead of decaying to a neutral `anylist`. The multi-input rungs of the append ladder — Concat Lists, Interleave, XSTACK — and the rank-changing TOCOL/WRAP are opted in too, via `agree` over their rows and a rank projection respectively; see passthrough.ts / subsystem-invariants "Passthrough"), Unique, Set (two-list set ops — `SetOpNode`, op selector: union/intersection/difference (A∖B)/symmetric difference; compares by value, dedupes + preserves first-seen order like UNIQUE, ignores nulls, errors survive except in intersection; Solenoid-native — Excel ships only UNIQUE), Set relation (two-list set PREDICATES → logical TRUE/FALSE — `SetRelationNode`, op selector: equal/subset (A⊆B)/superset (A⊇B)/disjoint; same value-set semantics as Set; both unwired → null; the companion to Set), Diff, **Concat Lists** (the 1-D rung of the APPEND LADDER, appendLadder: N extensible element-agnostic `anylist` rows joined in row order; a scalar widens to a 1-element list), Interleave, Pad, Shuffle, NthElement, Index, Length, Contains, NthValue, ArgMin/Max, SumProduct, Running (merged Cumulative + Rolling 2026-08-09: one `RunningNode`, a SegToggle picks the window — "Cumulative" grows it from the start, "Last N" slides it and shows a Window size input; 7 ops sum/avg/min/max/median/product/stdev; formula surface is ONE function RUNNING(op, list, [window]) — the aggregator is a string argument per aggregatorsAreArguments, the per-op RUNNING* family eliminated 2026-08-10), **Rank & Percentile** (ONE order-statistics node, nodeCombiningRound1: LARGE/SMALL, RANK.EQ/AVG, PERCENTILE/QUARTILE/PERCENTRANK with the INC/EXC forms as ops — all list + one scalar → scalar, RANK↔PERCENTRANK share the Value key), Normalize (0–1 / z toggle), Standardize, Correl, Covariance, Fisher, and the Regression subcategory (LINEST (linear / exponential toggle — absorbs LOGEST; the three outputs retitle slope/intercept/R² ↔ m/b/R²-log, keys unchanged so cables survive the switch), FORECAST.LINEAR (linear / exponential toggle — absorbs TREND and GROWTH; a scalar or list X, result mirrors its shape), STEYX, Poly Fit, Grid Interpolate). **Transform leaf (2026-08-23, the numpy/pandas/R batch — every one also a formula):** DIFF (Δ / % / ∇ gradient toggle), Shift (blank / wrap = numpy.roll), Bin (digitize), EWMA, Convolve, Integrate (trapz), Run Lengths (rle → value/count table); Build: Combinations (combos / perms toggle, 10k-row cap). Toggle cards share `makeToggleNodeComponent` (standardNode.tsx). **python-r-gap Tier 1 (2026-08-23f, see `python-r-gap.md`):** Aggregate ops PTP/IQR/MAD/SEM/CV/RMS; Correl ops SPEARMAN/KENDALL; Bin quantiles mode (NTILE); Outliers (z / IQR / MAD → one Value/Outlier frame, C5); Spectrum (FFT); Text Similarity + Fuzzy Match (Text ▸ Measure & Encode); Forecast (ETS) (Regression); Hypothesis Test gained ANOVA / Kruskal–Wallis / Mann–Whitney / Wilcoxon / Fisher exact / KS / two-proportion z / binomial (table sockets for the k-group ops).

**Stats**: the ONE Distribution node (oneDistributionNode, below) covers the distribution families; **Hypothesis Test** is likewise ONE node (nodeCombiningRound1): Z.TEST / T.TEST (paired, equal var, Welch) / F.TEST / CHISQ.TEST as ops — every op emits a p-value, the two-sample ops share the a/b keys so switches keep cables.

**Finance**: TVM — ONE acausal Equation node covering PMT/PV/FV/NPER/RATE (wire four of {rate, nper, pmt, pv, fv}, the fifth solves; payment-timing dropdown swaps the locked relation; rate = 0 uses the exact limit form), Compound Growth (fv = pv·(1+rate)^nper; covers PDURATION/RRI) and Effective Rate (EFFECT/NOMINAL) as locked Equation presets, IPMT/PPMT, CumPmt, NPV and IRR (each with a Periodic/Dated SegToggle — Dated reveals a Dates input and IS XNPV/XIRR, nodeCombiningRound1), MIRR, depreciation (ONE node since nodeCombiningRound1: SLN/DB/DDB/SYD/VDB, per-op input rows), bond pricing (PRICE/YIELD, odd coupons, accrued interest).

**Distributions**: ONE Distribution node (oneDistributionNode, 2026-08-09): a distribution dropdown (normal, standard normal, **PHI** — φ, the standard-normal density — and **GAUSS** — Φ−½, the 0-to-x half-area, both single-input forms that moved here from the Math node 2026-08-25, t, chi-squared, F, beta, gamma, lognormal, Weibull, exponential, binomial, Poisson, hypergeometric, negative binomial) plus a form dropdown (CDF / PDF / PMF / tails / inverse; GAUSS carries the one "Φ − ½" form); the inverse trades the x input for a probability, a distribution switch swaps the parameter inputs. `DIST_SPECS` in `nodes/distribution.ts` is the SSOT. BINOM.DIST.RANGE stays its own node. Z/T/F/Chisq tests unchanged.

**Text**: Text Input, case/trim transforms, CONCAT, TEXTJOIN/SPLIT, LEN, FIND/SEARCH, SUBSTITUTE/REPLACE, TEXTAFTER/BEFORE, **Regex** (REGEXTEST/EXTRACT/REPLACE), VALUE/NUMBERVALUE, TEXT/DOLLAR/FIXED, ROMAN/ARABIC, CHAR/CODE, ENCODEURL. **Every element-wise text node broadcasts (2026-07-25):** operands are `strcombo`/`numlist` and a wired list spills element-wise, Excel-array-formula style (`=UPPER(A1:A10)`) — see the Element-wise operand rule below. *(Removed 2026-07-25: **Text Map** ("UPPER (list)") — Text Transform with a list wired in IS it now; old saves load it as a Placeholder.)* The exceptions are deliberate: CONCAT/TEXTJOIN REDUCE a set to one string (Excel's CONCAT flattens an array rather than spilling), TEXTSPLIT is already 1-D → 1-D (broadcasting would need a rank-2 result the lattice has no 1-D→2-D edge for), and Regex stays on the wildcard ladder because its element type depends on the op — though it now emits `anycombo` rather than `any` (2026-07-25), so its dot no longer draws a scalar circle on a port that can spill a list.

**Date & Time**: TODAY/NOW, DATE/TIME construct, date parts, WEEKNUM/WEEKDAY, DATEDIF, **Workdays** (ONE node, nodeCombiningRound1: WORKDAY/NETWORKDAYS as inverse forms — the op swaps Days↔End date and retypes the output date↔number via `retypeOutputCables`), date formatting.

**Complex numbers**: COMPLEX build/unpack, 16 unary ops, 4 binary ops, IMPOWER.

**Build Frame** / **Frame from Lists** — the matrix⇄frame constructors, both TYPE-BY-ADOPTION (2026-07-15). "Slap headers on a table → Frame" (Build Frame) and "N typed lists → a mixed Frame" (Frame from Lists); adding columns is other nodes' job. Their value inputs are **adoptive** sockets (`adoptiveTableIn`/`adoptiveListIn` — an `AdoptiveSocket` with an `anytable`/`anylist` BASE instead of `trueany`): they accept any element family AND adopt the wired cable's concrete type via `settleWildcardTypes`, so a `datetable`/`datelist` → **date columns**. This is the only way `date` survives — a serial is indistinguishable from a number at the value level (`colTypeForSocket` maps the adopted socket → `FrameColType`; `null` for a not-yet-adopted `anytable`/`anylist` or `complex` falls back to value inference). `typedColumn`/`buildFrameTyped` (core `frame.ts`) do the typing; a numeric matrix still routes through the unchanged `buildFrame` (byte-identical). Headless (`run-graph.ts`, no settle pass) degrades to value inference (date→number). The old `columnFromCells` + Frame-from-Lists' "dates arrive as numbers, retype downstream" limitation are GONE.

**Split Frame** (`SplitFrameNode`): Frame → Matrix + Headers, with a **column-type filter** SegToggle (All / Num / Date / Bool / Text). Filtering to a type pulls just those columns out of a MIXED frame (which plain "All" can't — any text column makes the number matrix null); both Matrix and Headers are filtered. **The Matrix output socket type tracks the filter** (`splitMatrixOutput` + in-place swap via `applySplitColType`, like Get Column's read-as): number/all → `table`, date → `datetable` (serials), bool → `logicaltable` (1/0), text → `strtable` (a real string matrix, not null). `colType` persists via extractInit.

**Frames / Table verbs** (`nodes/frame.ts` + `frameVerbs.ts`; the relational family): Get Column (read-as retype incl. Boolean), Get Row, Add Column, Frame Filter, Frame Sort, Join (incl. as-of + semi/anti modes), GROUPBY, Append, Distinct, Head, PIVOTBY, Unpivot, Nest, Unnest, the cleanup verbs (Fill Down, Replace Values, Drop Blank Rows, Keep Columns / Drop Columns, Rename, Split Column, Merge Columns, Add Index (incl. the TWO-WAY option, author 2026-07-16: the same data indexed on BOTH axes as a coordinate-bordered matrix — exactly the grid Surface / Contour / Grid Interpolate read; a wired matrix widens to Col1…N and gets row + column indices counting from Start), Headers), Reconcile, and the cube family (Nest Join, Build Cube, Cube Columns, Cube Rollup). **Frame XLOOKUP `search_mode`** decides which row wins on DUPLICATE keys: "first" (default, Excel's mode 1) scans top-to-bottom, "last" (mode −1) bottom-up; Excel's binary modes (2 / −2) are deliberately omitted — on a materialized column a binary search over sorted data finds the SAME row a linear scan does, a pure speed knob with no distinct result here, and we always scan linearly for correctness. **Reconcile** classifies each key as added (right-only) / removed (left-only) / changed (a shared column differs) / unchanged with before/after/Δ per shared numeric column; when both a price and a quantity column are named (numeric on both sides) it decomposes the total (price×qty) change into the textbook three-term PRICE/VOLUME/MIX variance — price = (P1−P0)·Q0, volume = (Q1−Q0)·P0, mix = (P1−P0)·(Q1−Q0) — which sums EXACTLY to P1·Q1 − P0·Q0. **Computed Column** (`ComputedColumnNode`, tableRefSemantics/noPerCellFormulas): adds ONE defined column mid-pipeline — an inline formula or a wired λ; a bare name (or `[Name]`) is the WHOLE column as a list, `@name` / `@[Name]` is this row's cell; λ params stay row-bound; free names grow `anydata` SIDE-INPUT sockets (`sideVars`, persisted) with per-variable column-binding pickers; `addAs` types the result, `After` places it; one definition per column, never per cell. This node is WHY frames stay out of formulas (matricesInFormulas): the row iteration lives here and the formula only ever sees scalars (author direction 2026-07-29 — the frame stays pure data, the computation is a graph citizen). Per-row contract mirrors the broadcast rules: an error cell in any BOUND column propagates to that row's output (first in binding order); a null flows INTO the formula (ISBLANK/IF can see it — a formula is not an element-wise op); the output column's type is inferred from the computed cells, and a `Name (unit)` header tags the unit like Add Column. The same definition model lives on Frame Input's per-column source picker (**Data | Formula | λ**) — both surfaces run `computedColumnCore.ts`, so they cannot disagree. **Query** = a Composite pre-shaped for data transformation (verb chain inside, Manual-refresh mode — the Power Query shape, queryIsCompositePreset).

**2D Tables / Matrix**: Table Input grid (a LITERAL source on Frame Input's model: raw text is the stored truth, bad cells derive to NaN with the quiet affordance, the grid editor edits Source text; ONE element type per table via a Num/Text/Date/Bool SegToggle that retypes the output socket in place — header accent tracks it), MMULT, MDETERM, MINVERSE, MUNIT, TRANSPOSE, **VSTACK/HSTACK** (the 2-D rungs of the append ladder, appendLadder: N extensible `anytable` rows stacked in row order; a list counts as ONE ROW; ragged inputs pad with #N/A cells like Excel), WRAPROWS/COLS (leftover cells pad with #N/A, Excel's default pad_with), TOCOL/ROW, CHOOSEROWS/COLS, **TAKE / DROP** (one rank-preserving card: list, matrix or scalar in, same rank out; the sign of the count is the direction, 0 = omitted arg — the old 1-D and 2-D spellings merged, D6), **EXPAND** (grow to R×C; wired Fill or #N/A; shrink is #VALUE!), ROWS/COLUMNS, DIAGONAL (numpy.diag; off-diagonal 0 or blank, MUNIT's toggle), OUTER, Cross Product (3-vectors), TRACE / MATRIXRANK / NORM (MDETERM card ops; its op switch now retypes the output number ↔ table), Solve A·x = b, Eigen (symmetric, Jacobi). (Node kind `table` — vermilion accent matching the table socket; see `kind.ts`.)

**Input/Control**: Number Input, BooleanInput, Slider (its ◄/► step buttons are the "stepper"), AngleDial, **Date Input** (renamed from Date Picker 2026-08-21; type any date — ISO, day-first numeric, ordinals, month names, via the shared chrono-backed `parseDate` — or the native calendar; renders DD-MMM-YYYY and keeps the raw text; an ambiguous numeric like 3/4/2026 → `#AMBIGUOUS!`, never a guess), **XYPad** (drag a 2D handle → X/Y in [0,1]), **ColorPicker** (RGB / HSV sliders with gradient-painted tracks, or a Hex text field — a third mode; output-format dropdown hex/rgb — CSS-valid only, no `hsv()`/`hsl()`; output socket sits on the swatch row below the dropdown; conversions via the `colord` lib; feeds Chart Builder's Color), **ColorBlend** (two CSS color-string inputs — typeable or wired — × a blend-mode dropdown: mix/multiply/screen/overlay/soft·hard-light/darken/lighten/difference/exclusion/dodge/burn, W3C separable formulas per RGB channel, A = backdrop; hex out on a ColorPicker-style swatch row; unparseable input → `#VALUE!`; `colord` gained the `names` plugin globally so "tomato" parses; kind `string`), Slicer, Date Range, **Save Times** (last autosave + last write-to-file as two date serials, per-document, read through the leaf `saveTimeStore` seam; Refresh and Save buttons on the rows), RandBetween, RandArray, Sequence.

**Output**: Display, **Cast** (universal type conversion — number/text/date/complex/**boolean** ("Boolean" target → a real `logical`: "TRUE"/"FALSE" text or a nonzero number, via `coerceLogical`; added 2026-06-24), element-wise on lists; fully replaces the old single-purpose TEXT/VALUE/Format Date coercers, which were removed 2026-06-19 — old saves using them load as Placeholders). (The Progress node was removed 2026-06-15 — the Gauge supersedes it; old saves referencing it load as Placeholders.) **Visual nodes** (`nodes/visual.ts`, `display` kind, all pass-through, drawn with recharts): **Sparkline** (inline line/column/win-loss of a list), **Chart** (11 kinds: column/bar/line/area/pie/radar/radial-bar/funnel/scatter/composed/bubble), **Gauge** (a value on a fixed scale, a Dial/Bar style selector: a radial dial reading the value as a fraction — 1 = 100%, fixed 0→100%, minifies to a square — or a horizontal bar on a 0→Max track with a target tick, which ABSORBED the former Bullet node 2026-08-24; like 7-Segment it emits a chart VALUE, not a passthrough, so a Report embeds the readout — old Bullet saves load as Placeholders), **Heatmap** (Table in → cool→warm color grid out). **Mermaid** (`MermaidNode`, wide card) is the one that ISN'T recharts: mermaid.js source text in (typed on the card or wired via the `source` socket) → a `MermaidValue` (sibling brand to `ChartValue`, `mermaidValue.ts`) out the **`chart` "Special" socket** — so it embeds into a Report inline exactly like a chart. Rendered by `MermaidView.tsx`, which dynamically imports mermaid (heavy: d3/dagre) only when a diagram is on screen. This is the **standing rule** in practice: rich visuals are NODE outputs flowing the chart socket, never Report-markdown features (Reports stay plain text + embeds). LAMBDA values wired into a Report likewise render as KaTeX via the same inline-ref path (`inlineRefDisplay.tsx`). The chart op pickers use `SegToggle` (the Format Controller's segmented button selector), not a dropdown; hover shows a `formatScalar`-rounded readout, and the ⤢ expand button is the shared `solenoid-expr__expand`. The figure catalog has since grown past this list — Boxplot, Candlestick, Contour (Surface's Flat view since nodeCombiningRound1 — one node, two leaves), Waterfall, Waffle, Calendar (heatmap), Vector Field, 7-Segment (a flat seven-segment readout — lit segments in the accent, ghost segments barely-there, no flap/LED skeuomorphism, no animation, per DESIGN.md; emits a chart VALUE, not a passthrough — author 2026-07-16 — so a Report embeds the readout inline), **Record** (frame rows as labeled boxes on a text-defined grid, THREE views on one op selector — Card: the picked row, pager or wired 1-based `Row`, with the resolved pick echoed out `picked` so downstream follows the pager; Gallery: every row as a tiled card; Board: lanes keyed by a `Group by` column, blanks last as an `—` lane, the grouping column auto-skipped from default stacked cards; gallery/board cap at 60 cards with a `+N more` line. One layout line per grid row, cells split on `|`, a repeated name merges its cells; a string cell holding a `data:image/` or image-extension URL draws as the picture; the CARD never draws the grid — it carries the [Chart] chip, and the drawn card lands where the chart output goes: a resizable Display, the popup, a Report embed; DOM view `RecordCardView` in `chartCards.tsx` (boxes touch, square), seed `record-cards`, 2026-08-18), Surface, the Proportion CATEGORY (the Proportion node with Treemap / Waffle layouts on an `op` selector, plus Sankey), plus the draw-your-data inputs (Point Plotter, Curve, Grid Painter) — all flowing the same `chart` socket. Sparkline/Chart/Heatmap use the wide card (`nodeWide`) so the plot fits; Sparkline + Chart share one `ChartView` (`components/chartView.tsx`) and an expand-popup (`chartPopupStore` + `ChartPopup`, mounted in App, like FormulaPopup/TablePopup). **Eleven figures (Chart, Histogram, KPI, Gauge, Proportion, Sankey, Waterfall, Candlestick, Boxplot, Calendar, Record)** have an `options` string socket — typeable inline (an InlineInputs text row) or wired from the **Chart Builder** node, a labeled "Concat for chart options" (each field also a wireable input) that emits a flat `key=value;…` string — its chart-type dropdown (`CHART_BUILDER_TARGETS` in `chartOptions.ts`, 2026-07-20) shows just the option rows the chosen figure's renderer reads (wired/valued rows stay visible dimmed; serialization stays full-width so one builder feeds several charts); an option a node's figure can't express (e.g. `marker` on a treemap) is simply inert, matplotlib-style. `title` overrides the node label as the figure title wherever it applies. Chart's `values` socket is measured-centered on the plot, the `options` socket is on its own row below (they used to overlap); Chart Builder's and Color's output sockets are measured onto their readout rows (`hideOutputSockets` + a `MeasuredSocketRow side="output"`). The string using **matplotlib's** kwarg names (`title/xlabel/ylabel/color/grid/marker/ylim/linewidth/markersize/alpha/fontsize` — `fontsize` in points, 10 = the built-in sizes; an FC on a chart socket multiplies on top with its display-only text scale, see `docs/format-model.md`). `nodes/chartOptions.ts` (`parseChartOptions`/`serializeChartOptions`) is the one source of truth; `ChartView` applies the parsed `ChartOptions`. That configurability is what distinguishes Chart from the deliberately-minimal Sparkline. Every family's combo socket (`strcombo`/`datecombo`/`complexcombo`/`logicalcombo`) mirrors `numlist` (scalar-or-list, bicolor split square).

**Find (under Lists)**: XLookup, XMatch. Convert (unit conversion) sits under Output. (Classic MATCH / VLOOKUP / HLOOKUP omitted — XLOOKUP/XMATCH supersede them.) **XMatch carries the same `search_mode` as the frame XLOOKUP** (First / Last — which duplicate wins; binary ±2 omitted for the reason above), so the two lookups and their formula forms all take the same two mode arguments; the shared kernel is `xmatchIndex` (`listOps.ts`).

**Connections & sinks**: Web Source, Local File (CSV/Parquet, reader by extension), Data Feed (keyed providers; auto-refresh), **Geocode** (place name → lat/lon/IANA timezone, Open-Meteo, keyless), **Weather** (Open-Meteo: past+future Daily frame + Now temp; °C/°F carries the unit downstream), **Holidays** (Nager.Date: a year's public holidays → frame + Dates list feeding NETWORKDAYS/WORKDAY + days-to-next; optional region), **Currency** (Frankfurter ECB FX; Amount/From/To → Converted authored with the target currency as an FC unit + Rate + As-of), Import HTML / Import XML, Import Obsidian Note; Write File (CSV/JSON) / Write to Obsidian (disk writes fire ONLY from the node's Run button — see subsystem-invariants "Sink nodes"). **Data Quality**: Expect (inline assertions that pass data through and report violations), Tornado (sensitivity sweep figure). **Canvas/meta**: Session History (live undo/redo log, non-persisting), Presentation (presenter-mode step list).

**Formula**: **Expression** node — type `a * b + 1`, named variables auto-become input sockets; full math-function scope + list broadcasting. **Per-variable explanations (2026-07-09):** each variable can carry a prose description (`varDescriptions` map, kept OUT of the formula string so KaTeX never renders it) — a hover tooltip on the card + an editable legend under the KaTeX in the formula popup; shared with **Equation and LAMBDA** (all three use the same formula popup), seeded on some pack presets (Ohm's Law, ideal gas…). A **LAMBDA wired into a Report renders its formula as KaTeX with a muted "where:" legend beneath** (the value carries the descriptions) — the math-renders/prose-explains split. The **lambda-helper family** is MAP / BYROW / BYCOL / REDUCE / **SCAN** / MAKEARRAY (each runs an inline formula or a wired LAMBDA value): **SCAN** (2026-07-10) is REDUCE's running-intermediate twin — the same row-major fold from Initial but it emits the accumulator after every cell, keeping the input's shape (a running total is `acc + value`, a running max is `MAX(acc, value)`); the fixed-op Running covers the common aggregates and REDUCE gives only the final value. **Polyform** (2026-06-19): a Number/Text/Date/Auto result-type selector lets it (and MAP/BYROW/BYCOL/REDUCE/SCAN/MAKEARRAY/LAMBDA) loop ANY Excel function over arrays of any element type — `UPPER(name)`, `first & " " & last`, `DATE(y,m,d)` — swapping the output socket to match; Expression variable inputs are `anydata` (scalar / 1-D list / 2-D matrix — the matricesInFormulas lift; frames and cubes stay out permanently, the verb engine is their surface, and a Computed Column carries row formulas). LAMBDA's capture sockets stay `anylist`. See the "Polyform" dev-notes entry. The names `pi`/`tau`/`e`/`phi` resolve to constants (`FORMULA_CONSTANTS` in `excelFormula.ts`), not variables — they're skipped by `extractVariables` and inlined by codegen + `evaluateSteps`, so the whole formula engine (Expression, LAMBDA, MAP/BYROW/REDUCE…) sees them.

**Script** (`ScriptNode`, `nodes/script.ts`, decisions scriptNode; the bounded form of out-of-scope §4): a JavaScript function expression as a node. Its parameter names are `anydata` inputs re-derived on commit (`applyScriptChange`, Expression's mechanic; `autoLiterals` so an unwired parameter takes a typed number or text, unwired-untyped is `undefined`, a wired blank `null`); the return value is folded onto the value model by `scriptCoerce.ts` and TYPES ITSELF — no declared result type: numbers/text/booleans are their own families, a `Date` or `Solenoid.date(serial)` is a date, rows of `{name: value}` objects build a FRAME with typed columns, and rows nesting rows/lists build a CUBE (NaN `#DOMAIN!`, unsupported values `#TYPE!`, ragged rows padded with null, `#SHAPE!` beyond the accepted shapes, a list/table/frame-column mixing families `#AMBIGUOUS!`); the result socket reconciles family and rank off the value. Inputs are trueany: a wired frame (materialized in full) or cube arrives as the same rows-of-objects (`scriptArgToJs`); lambdas/charts/documents error before the run. A volatile source (Math.random, Date.now, argless `new Date`, …) grows the shared Recalculate button (`scriptIsVolatile`). Runs in the sandbox Worker under a 1 s wall clock (`subsystem-invariants.md` § Script sandbox). Named Script, not Code: CODE is the Excel node (NAME-2).

**Equation** (`EquationNode`, `equationSolve.ts`, decision equationNode): the acausal sibling of Expression. Type a relation (`V = I * R`); every variable gets an input AND an output socket, plus a fixed logical **Check** (output key `holds`) output. Exactly one unknown (unwired, empty literal) -> solved: symbolic AST isolation when the unknown appears once (`+-*/^`, POWER/LOG, EXP/LN, SQRT, trig/hyperbolic, DEGREES/RADIANS, unary minus, %) -- the isolated form unparses to a formula and recompiles, so list broadcasting is free; otherwise a QUADRATIC-in-the-unknown residual (numerically probed, any arrangement) solves via the quadratic formula and yields EVERY real root as an ascending list (x² = 36 -> [-6, 6]; double root scalar; negative discriminant #SOLVE!), and anything else falls to a numeric log-grid + bisection fallback on the LHS-RHS residual (scalar-only, `#SOLVE!` when no real root). All variables known -> Check is a relative-tolerance (1e-9) truth check, broadcasting per-element. Principal branches on inversion (sqrt, ASIN). Wired variables pass through to their outputs, so the node doubles as a labeled junction.

**Schedule** (`nodes/schedule.ts` + the pure `scheduleCpm.ts`, frame kind, Analyze menu; 1.4 H6, 2026-09-06): the critical-path method over a tasks frame — Task (the `Task` column or the first text column; unique names), Duration (the `Duration` column or the first number column, days; blank/0 = a milestone), Predecessors (a comma-separated text cell; blank = none; finish-to-start only), an optional Project column for gantt sections; `start` (dateIn, wire-only; unwired = today; a wired blank schedules nothing), `mode` Working days (Mon–Fri minus the `holidays` date list) | Calendar days. Kahn order, forward + backward pass in working-day INDEX space (a `Calendar` maps index → serial once, so skipped days are never re-counted). Outputs: `frame` = the rows in order + `Start · Finish · Float · Critical` (a milestone sits ON its predecessors' finish day), `finish` = the last finish (date), `gantt` = Mermaid source (`excludes weekends, <holidays>`, `section` per Project, `crit` / `milestone` tags, exclusive end dates). A cycle, unknown predecessor, negative/non-numeric duration or duplicate name → one `#VALUE!` naming the task on every socket. Eager verb, no Polars twin. Seed `remodel-gantt.json` (Holidays node feeds the skip list); tests `scheduleCpm.test.ts`, `nodes/schedule.test.ts`. Not yet: a cube in (Predecessors as a list cell) — the Obsidian bundle's A′ follow-up.

**Decision support**: **Decision Matrix** (`DecisionMatrixNode`, frame kind, in the Table-verbs menu) — a port of the author's Decision Matrix Bases View Obsidian plugin. Scores + ranks a Frame of options (rows) by criteria (number/logical columns — NOT date; an optional leading text column names them): weighted average `Σ(value × weight) / Σ|weight|` (a NEGATIVE weight penalizes a lower-is-better criterion), then competition rank ON THE ROUNDED (4dp) score — display and rank can never disagree (decisionMatrixFamily). **Weights ride a criterion-keyed `weights` FRAME** (`Criterion · Weight · Norm`; orderedColumnsAreFrames — the DM's weights are orthogonal to the score rows, one per criterion COLUMN, so they key by criterion NAME, never a positional list): `resolveDecisionWeights` (`frameVerbs.ts`) aligns it to the detected criteria by name — first text column = Criterion; the number column named Weight/Value (else the first number column) = the weight; an optional Norm text column parsed by `parseNormalize` (Raw/none, ÷Max/max, Rank). Unwired, or a criterion the frame omits → weight 1, the default normalize. The card keeps only the node-wide defaults: the `normalize` SegToggle (**÷max is the node default** — decisionMatrixFamily: raw silently degenerates the moment criteria mix scales; `none`/`÷max`/`rank`, where **÷max and rank both land in [0,1]** so mixed-mode columns stay comparable: weights, not scale, decide influence) and `detail` (Summary / Breakdown — Breakdown inserts each criterion's **signed contribution**, effective × weight / Σ|weight|, so the columns sum to the Score and a negative-weight column reads as the penalty it is; never the bare post-normalize value). **Decision Sensitivity** (`DecisionSensitivityNode`) is the companion: feed the same Scores + a Scenarios frame — the weights frame widened: rows = criteria, each NUMBER column = one scenario named by its header, one optional shared Norm column; a criterion a scenario omits → 1, but NO criterion row matched at all → `#VALUE!` (the renamed-criteria trap) — and it emits a **Cube** — Scenario · Winner · Margin · Ranking, where Ranking is the full ranking frame nested per cell (drill in). Margin = top − runner-up (decisiveness); a dead tie for first lists every tied option in Winner ("A = B"). Verbs `decisionMatrix` / `decisionSensitivity` / `resolveDecisionWeights` in `frameVerbs.ts` (`decisionMatrix.test.ts`); static output shape declared by the node's own `frameShape()` (label · [criteria] · Score · Rank). Worked-example seed `decision-matrix.json`, held to the engine by `decisionSeed.test.ts` (winner order, the flip, the exact tie, the report wiring): one criterion comes from a Note's frontmatter FRAME (rows-of-objects: prose = the judgment, rows = the data) left-joined by name, the weights and the transposed scenarios are Frame Inputs, the podium is a Columns node (keep [Option, Score]) → Chart (labeled bars) beside a radar off the Join (chartRadarTranspose), and a Report memo pulls `=winner` (INDEX 1,1), the podium figure, and the contributions table.

**python-r-gap Tier 2 (2026-08-23g, see `python-r-gap.md`):** Lists gained Smooth (Savitzky–Golay / LOWESS / Gaussian, op-owned parameter sockets), Find Peaks, ARGMAX ops ARGSORT / ARGSORT DESC / WHICH (sockets retype with the op), Polynomial Roots (Complex); Finance gained Returns (log / simple / cumulative, drawdown, max drawdown, CAGR, volatility, Sharpe, Sortino — op owns rf / periods); Regression gained Decompose (classical seasonal); Text gained Pad Text, Truncate Text, Template ({name} grows a socket), Hash, UUID, UNACCENT / SLUGIFY on Text Transform and Base64 on the url-encode card; Tables gained K-Means, PCA, Logistic Regression (Analyze), Bind Columns (Append's positional sibling — a backend verb on both engines) and Join `how = cross`.

**Describe / Correlation Matrix / Window** (Table verbs, 2026-08-23f): pandas describe (one row per column), df.corr / df.cov (Pearson / Spearman / Kendall / covariance, pairwise-complete), and **Window** — the per-group transform column (running / rank / lag / lead / diff / pct_change / rolling / group total / share / first / last, partition + order, original row order kept); a lazy FrameOp — Polars `.over()` on desktop, oracle on web, `window.json` parity fixture. Date & Time gained Epoch ↔ Date and Truncate Date; Finance gained Amortization Schedule. **Aggregation**: **Group Lists** — 1D parallel-list group + aggregate (keys + values outputs). **GROUPBY** — group a frame's rows by key columns + aggregate. **SUMIFS** (filterOneJob, amended 2026-07-09) — conditional aggregate over ONE FRAME: op selector (SUMIFS/COUNTIFS/AVERAGEIFS/MINIFS/MAXIFS), a Values-column field (hidden for COUNTIFS), extensible criteria rows (column + test + value, AND like Excel's *IFS); empty-match parity (AVERAGEIFS #DIV/0!, MINIFS/MAXIFS 0); missing column → #REF!; parallel lists route through Frame from Lists (the 2026-07-06 aligned-columns rule). **PIVOTBY** = full Excel `=PIVOTBY` (2026-06-29): multi row/col/value fields (composite `"East | A"` headers), per-value function from the expanded set (SUM/AVERAGE/COUNT/MIN/MAX/PRODUCT/MEDIAN/MODE/STDEV.S-P/VAR.S-P/PERCENTOF), grand+subtotals (row/col depth, top/bottom) that **re-aggregate the source**, value/field sort, filter mask, and % running totals (`relativeTo`). Engine = `pivotFrame(f, PivotSpec)` in `frameVerbs.ts`. See dev-notes "PIVOTBY — full Excel parity".

**Annotation**: **Note** (free-floating sticky note — markdown title/body, drag, tint. Sockets-less BY DEFAULT, but if its body opens with an Obsidian-style `---`-fenced YAML frontmatter block, each key becomes a typed OUTPUT socket — guessed type persisted/overridable per-key — so the Note doubles as a typed-record / constants source; a key whose value is rows of inline objects (`- {k: v}` block or `[{k: v}, …]` flow) emits a **frame** output, columns from the row keys — the Script node's `{name: value}` row shape, so a Note can seed a table with no Frame from Lists; `noteFrontmatter.ts`, dev-notes 2026-06-23). **Image** (free-floating picture, in the Add menu's "Other" category — attach a local file or paste a web URL, height field, no socket; sockets-less, no data). **File Link** (in "Other" — a link to a file on disk: title, preview row + Open button, no sockets, no data. Stores the PATH not the bytes. Desktop persists the absolute `path` and Open launches it in the OS app via `openPath` [needs `opener:allow-open-path`]; web attach is session-only [browser has no path], only `fileName` survives reload. Fixed-width card — deliberately not a SIZE_OWNER). **SVG** (interactive picture / visual slicer, also in "Other" — `SvgPickerNode`, kind `input`; see below). **Format Controller** (catalog label "Format") — docked node setting a socket's number format, and AUTHORING a unit only onto a unit-less value; fed an already-united value it MIRRORS the unit and LOCKS (firstClassUnits — only Convert changes a unit). `formatModel.ts` is the control truth table.
- **SVG Picker (`nodes/annotation.ts` `SvgPickerNode`, `components/SvgPickerNode.tsx`, 2026-07-08):** load an SVG (local `.svg` read as TEXT, or a web URL fetched to text — inlined into the well so its inner shapes are hoverable/clickable). **Click a shape/layer → the `Layer` string output emits its NAME** (element `inkscape:label`/`data-name`/`aria-label`/`id`, human-label-first, walking up to the nearest named ancestor — pure `svgLayer.ts`, `resolveLayer`); wire that into a Filter to slice a dataset by the clicked region (a clickable map/floorplan as a data selector). Clicking the current pick clears it; the pick is `null` (missing) until made. ALSO flows the picture out the `chart` object socket as an `SvgValue` (`svgValue.ts`, source + selection + hover color) so a Report / Display / Cable Switch / Composite boundary renders it via the shared read-only `SvgFigure.tsx` — highlighting the same layer. Hover/selection highlight = an imperative `filter: drop-shadow` glow in the adjustable color (not per-move React state). Persistence: the markup lives in `stringLiterals.source` (Mermaid pattern — text, no bundling, unlike Image); `url`/`hoverColor`/`selectedLayer` round-trip via `INIT_FIELD_ORDER`. **Caveat:** cross-origin URL fetch can be CORS-blocked (local-file attach is the always-works path); a failed fetch leaves an inline hint. `svgLayer.test.ts` + `svgPicker.test.ts`.
- **Image persistence (`nodes/annotation.ts`):** a web `url` round-trips through the JSON save (and copy/paste) via `extractInit`; a LOCAL file is read to a base64 `dataUrl` that is deliberately NOT persisted (the save is plain JSON — no embedded image bytes yet; bundling comes later), so it's session-only and `dataUrl` is kept out of the extractInit whitelist. `src` getter = `dataUrl || url` (a fresh local attach wins; setting one source clears the other). The height field uses `useDraftCommit` (commit on Enter/blur), not live onChange — a live-clamped number input can't be cleared to retype.

**Conduit**: block bundler (up to 8 lanes, one rotation angle) whose outputs travel as a single Ribbon cable. (The old two-arm "Manifold" bundler it replaced was removed 2026-06-19.)

**Composability rule**: scalars → fine-grained one-op nodes. Lists/tables → bundled task-shaped nodes (e.g. one Aggregate with op selector, not five separate sum/avg/etc. nodes).

**Node design rules (the author's standing shape calls):**
- Scalars → fine-grained one-op nodes; lists/tables → bundled task-shaped nodes with op
  selectors. A variant is a mode/op selector on the existing card, never a sibling node
  (decisions maximalMerge).
- Aligned parallel columns → ONE frame input, not parallel list sockets (charts, SUMIFS,
  the frame verbs). The same for OUTPUTS: correlated lists (t and y of a solution, the
  parts of a decomposition) leave as ONE frame, never as parallel list sockets (author,
  2026-08-24). This is about ROW-aligned data — a value per row, parallel to a frame's rows
  (why the Allocator's per-category weights are a Weight COLUMN, not a socket). A vector that
  runs ORTHOGONAL to the frame — one value per COLUMN, like Decision Matrix's per-criterion
  weights — is not parallel data and legitimately stays a list socket.
- A node that takes a user formula takes it as a LAMBDA input (`lambdaIn` + `lambdaSig` +
  `resolveFn`, the λ-family in `tableLambda.ts`), never as a string socket holding an
  expression (author, 2026-08-24).
- Meaningful differences from Excel, especially output-affecting ones, go in the catalog as
  `parity: false` + a note (`nodeExcel.ts`).

**Labeled-slots vs list-input rule** (the variadic-node design call — see `docs/archive/node-arity-audit.md`): a variadic node uses **individually-labeled, individually-wireable scalar input rows** (the `ExtensibleInputs` / `PairedExtensibleInputs` pattern) — NOT a single list/table socket — when each input plays a **distinct role**: positional (CHOOSE's value-per-index) or **paired** (IFS/SWITCH's condition↔result). The label IS the affordance — it tells the user what each slot does and what their edit affects; a raw list of paired/role-distinct values hides the cond↔result relationship and makes edits opaque. Per-slot sockets also let each input come from a **different** upstream node. Use a single **list socket** only when the elements are **interchangeable** — all the same role, where order may matter but identity/pairing does not (SUM/AVERAGE/AGGREGATE, List literal). Litmus test: if you'd have to write "the 2nd element means X, the 3rd means Y" to explain the list, it should be labeled slots instead.

**Input-dimensionality rule** (audited end-to-end 2026-06-19 — every node input checked against its `data()`; the socket type both gates connections via `canConnect` AND drives engine-boundary coercion in `coerceInputs.ts`, so it must match what `data()` actually consumes). Pick an input socket by the input's ROLE, not by what "feels related":

- **Element-wise operand** (paired with each element through a broadcaster) → the family's COMBO: `numlist` / `strcombo` / `datecombo` / `logicalcombo` / `complexcombo`. This is an *operand*, so per-element vectors are valid and intended (Excel array-formula parity, e.g. `=ROUND(A1:A10, B1:B10)`, `=LEFT(A1:A10, B1:B10)`). Covers ArithmeticNode's `a`/`b`, the secondary operands `RoundN.digits`, `Clamp.min`/`max`, `MRound.multiple`, `Gcd.b`, every date operand (DatePart/DateAdd/DateDiff/DATEDIF/WORKDAY/NETWORKDAYS/DATE/TIME), every text operand (UPPER/LEN/LEFT-MID-RIGHT/FIND/SUBSTITUTE/REPLACE/REPT/CHAR-CODE/TEXTAFTER-BEFORE/EXACT/NUMBERVALUE/ROMAN-ARABIC/FIXED/DOLLAR/ENCODEURL/Reverse Text/Spell Number), and every complex operand (COMPLEX/IM Unpack/the IM unary + binary ops/IMPOWER/Quadratic Roots). **All five families are swept as of 2026-07-25** — a combo rung with no node uses is now a bug, not a gap. Do NOT narrow these to the scalar rung — that would break list-broadcasting and make them inconsistent with the rest of their family. The combo is a pure WIDENING: it accepts everything the scalar accepted and feeds everything the scalar fed (combo→scalar is an explicit lattice edge), so switching one breaks no existing cable.
- **Structural control param** (a count, index, window size, polynomial/Bessel order, numeric base, MODE/type flag) → `number` (or scalar `string`). It has no per-element meaning, so a list there is a genuine mistake the socket should block. The test is whether the value describes ONE operation over the whole input or varies per element: `Take.count`, `Running.window`, `Combinatorics.n`/`k`, `BaseConvert.from`/`to`, `Slice.start`/`end`, distribution evaluation points/parameters, all finance scalars (rate/nper/pmt/pv/fv/basis…), WEEKDAY's `return_type`, YEARFRAC's `basis`, WORKDAY's `weekend_code`, NUMBERVALUE's decimal/group separators. A per-element numeric operand is NOT this — `LEFT.n`, `MID.start`/`len`, `REPLACE.start`/`num_chars`, `REPT.times`, `FIXED.decimals` are combos.
- **1-D list / sample data** → `list` (`strlist` / `datelist`). Stats samples, cashflow arrays (NPV/IRR/XIRR…), Filter's `mask` include-vector.
- **2-D matrix** → `table` (numeric), `strtable` (text), `datetable` (date), `complextable` (complex); **named-column data table** → `frame`. Matrix ops take `table`; frame ops take `frame`. Any 2-D OUTPUT is blocked from narrowing into a 1-D/0-D input. A lower-rank value WIDENS UP: a list → a matrix/frame, a matrix → a frame (a list → a single ROW). **Element-agnostic 2-D inputs use the GRID socket `anyTableIn`** (TRANSPOSE, HSTACK, CHOOSEROWS/COLS, reshape-flatten, MAP/BYROW/REDUCE-values) — they render as a grid and a 1-D list widens in. TableInfo (ROWS/COLUMNS) is `frame`-typed (a matrix/list/scalar widens in). See subsystem-invariants "Socket lattice". **Frame edges (2026-06-24):** Get Column gains a **read-as Boolean** (a logical column exits as a real logical list; 0/1 + true/false coerce); **Frame Input** is a LITERAL source for Data columns — stores the raw text you typed, derives the typed frame at compute, and the popup's **Source checkbox** shows that raw text on every frame node (`FrameColumn.raw`) — and since the column-source model (2026-07-29/31) each column also has a SOURCE: **Data** (typed cells) | **Formula** (an inline row-wise expr; `expr?` on `FrameSourceColumn`) | **λ** (a wired lambda; `lambda?`, which wins when both are set). Computed columns evaluate in topo order with cycle refusal, may carry a per-column UNIT from the popup's unit picker, and the chip's **ƒ** marks a defined table. The popup's **Form view** (frame-source editors only, 2026-08-18) edits ONE record as labeled fields — pager, + Record / − Record — riding the same raw-text grid truth and edit-draft path as the grid cells, so its cursor reaches rows past the grid's 1000-row render cap; computed columns render read-only there. Field placement is the SAME Record layout text (`parseRecordLayout`; empty = stacked, unmatched names keep inert boxes, columns off the layout are hidden), and the form renders as the record-card look made editable (touching square boxes, label in-box). The layout is authored on the Frame Input CARD (the Record card's textarea pattern — never in the popup) and persists as `FrameInputNode.stringLiterals.layout` (the declaration is the load gate). See subsystem-invariants "Frame Input is a LITERAL source".
- **Recursive nested table** → `cube` (the lattice supremum: a frame whose cells hold ANY value, incl. a nested frame/cube). Producers: **Nest Join** (nest two frames on a shared key -> a cube of sub-frames; tidyr nest_join, the dual of a flat Join) and **Build Cube** (extensible `trueany` cells -> one column, each cell any value). Accessor: **INDEX** is `trueany`-in/`trueany`-out and reads a cell out of a list/matrix/frame/cube (a nested frame/cube comes out whole). **Whole-axis form (2026-07-15):** a blank/0 Row = the whole COLUMN and a blank/0 Column = the whole ROW (Excel's `INDEX(range, 0, col)`); both blank passes the container through. Slice shapes follow the existing accessor conventions — frame row → a ONE-ROW FRAME (Get Row), frame column → the values LIST (Get Column), cube slices stay CUBES (nested cells whole), matrix slices → 1-D lists. The Row/Column fields default empty with an `[all]` placeholder. **Cube Rollup** (`CubeRollupNode`, 2026-07-03) is the reverse direction: aggregate a column INSIDE each row's nested sub-frame (the full 12-op AggOp set, reusing GROUPBY's `aggregateGroup`), flattening the cube back to a Frame with the roll-up appended — the BOM/nested-costing shape ("cost of an assembly = SUM of its nested parts"); see the rollup row in the `cubes` seed (the dedicated bom-costing seed was retired 2026-07-08). Display: `CubeDisplay` + the drill-in `CubePopup` (depth shown; every nested cell — cube/frame/list — drills in place via one breadcrumb). Cached `depth` counts cube-in-cube only (a nested frame is a leaf). Full detail: `docs/archive/cube-node-scope.md`.
- **Genuinely type-agnostic** → the WILDCARD LADDER (wildcardLadder, 2026-07-09): `any` is an element-agnostic SCALAR (gray circle — one value of any family), `anylist`/`anytable` its 1-D/2-D siblings, and **`trueany`** (a HOLLOW gray circle, border only) is the supremum that accepts anything — Cast, Display, the Test inspector, Input Switch, unwired Conduit lanes, selectors, Report refs, Placeholder/composite ports. **Expression's variable INPUTS are `anydata`** (matricesInFormulas, 2026-07-28 — the rank-≤2 rung: scalar / list / matrix in, frames/cubes never; the ladder also holds `anydata` between `anylist` and `anytable`), while **LAMBDA's capture sockets stay `anylist`**; the polyform producers declare their OUTPUT element type with a Number/Text/Date/Auto result-type selector that swaps the output socket at the node's dimensionality (Auto = `any` at scalar/combo, `anytable` at matrix), and a MATRIX result swaps the family's matrix rung in place (retypeReconciles reconcile). (REDUCE's iterable `values` and the MAP/BYROW table inputs are `anyTableIn` grids; the scalar seed is `anylist`.) See wildcardLadder + the "Polyform" dev-notes entry.

Governing principle: **enforce TYPE separation (Cast to cross element families; `logical↔number` is the one bridge), allow DIMENSIONAL flow** (lower ranks widen into `anytable`/`frame`). See subsystem-invariants "Socket lattice"; machine-checked by the full-sweep `socketConnect.test.ts`.

## Packs (add-on node bundles — `src/graph/packs/`, 2026-07-09 wave)

Pack nodes live in per-pack files (`packs/electricity.ts`, …), NOT `nodeCatalog.ts` —
each pack file IS its inventory (formula entries + custom-node placements + FC
units/formats), and every pack has a vitest file asserting its formulas against
reference values. Registry/activation: `packs.ts`; authoring shapes: `packs/packShared.ts`.
The current built-in set — Geometry + Common Excel Timesavers ship ON; the rest ship OFF:

- **Geometry** (ON): 27 formula presets (areas/volumes, circles & arcs, solids) + HYPOTENUSE + **Triangle Solver** (the current EQUATION card: dual-socket hero rows per part + a logical Valid check + the triangle drawn to scale; the angle outputs carry the real `deg` unit per-output (`annotationFor`), so they read as degrees downstream (and a trig node in Auto mode picks them up) while the sides stay bare; `numlist` sockets like the rest of the Equation family — parallel lists broadcast to a triangle per index (Valid becomes a logical list, the figure draws index 0); any three parts incl. one side → all sides/angles/area/perimeter, over-given parts are consistency-CHECKED like Equation's all-known mode; SSS/SAS/ASA/AAS, ambiguous SSA is an honest #SOLVE!); DMS format, turn/px units.
- **Common Excel Timesavers** (ON): core-node reclassification tags + 7 presets (Percent Change, CAGR, Ordinal, Clean Whitespace, Mask, word/occurrence counts) + Reverse Text + Spell Number + **Time Zone Convert** + **World Clock** (pure `Intl`, DST-correct; timeZone.ts) + **QR Code** (text/URL/Wi-Fi/vCard → ImageValue out the chart socket; `qrcode` lazy-loaded, SVG build in qrCode.ts).
- **Electricity & Circuits**: 23 presets + Parallel Combine, E-Series Value, AWG Wire, **Resistor Color Code** (4/5-band dropdowns + a live band glyph → Ω + tolerance; IEC 60062); Electrical FC units + SI-prefix format. Ohm's Law and dBm ↔ Watts are locked EQUATION presets (solve any way, or truth-check).
- **Electromagnetism** (dependsOn electricity): 20 presets (Wavelength ↔ Frequency is an equation preset) + the CODATA Physics Constant node, whose unit rides downstream like an FC lock, + **EM Spectrum Band** (frequency OR wavelength → Radio…Gamma, visible names its color; emits both quantities via c).
- **Health & Fitness**: 20 presets (BMI/BSA/IBW, body fat, BMR/TDEE, cardio, clinical) + **Heart-Rate Zones** (age / optional resting HR / optional max override → a five-zone Low/High FRAME; Karvonen when resting is given — the pack's chartable table).
- **Fluid Mechanics**: 20 presets + the Colebrook root-finding friction-factor node + **Pipe Roughness** (13-material ε table, mm; with a diameter also emits ε/D for Colebrook/Swamee–Jain); pressure/flow/viscosity FC units.
- **Thermodynamics & Air**: 18 presets + ISA Standard Atmosphere + Antoine Vapor Pressure; Energy FC units. The old four solved ideal-gas forms are ONE pV = nRT equation preset.
- **Sets & Membership**: Is In (membership mask) + Tally (value counts); claims the core COUNT DISTINCT aggregate op. (Semi/anti join modes are core Join.)
- **Earth & Sky**: 8 presets (haversine, bearing, gravity, orbits) + NOAA Sun Position, Sunrise/Sunset, Moon Phase.
- **Chemistry Basics**: 11 presets + Element (118 IUPAC weights; the mass output carries g/mol per-output — `annotationFor`) + Molar Mass (formula parser); Chemistry FC units. Moles ↔ Mass and pH ↔ [H⁺] are equation presets. The Element card opens a PICKER popup: fuzzy search (symbol/name/number) + the clickable 18-column periodic table itself (symbols only; current pick and best match accented).

Composite-shaped pack ideas are planned, not built — `docs/pack-composite-plans.md`.
